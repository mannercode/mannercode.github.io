# `_posts/` 한국어 글 검토 보고서

- 검토일: 2026-06-11
- 대상: 한국어 원본 8편 (영문 번역본 `-en.md` 제외)
- 방법: 글별 언어/기술 이중 리뷰 + 시리즈 일관성·메타데이터 교차 검사 후, 전 항목을 원문 대조로 검증
- 확정 지적: 총 210건 (High 18 / Medium 75 / Low 117)

| 파일 | 건수 | High |
|---|---|---|
| 2024-02-03-principled-pragmatic.md | 29 | 3 |
| 2024-03-09-oop.md | 31 | 1 |
| 2024-05-04-ebi.md | 26 | 2 |
| 2025-04-01-backend-design-1.md | 14 | 1 |
| 2025-05-01-backend-design-2.md | 30 | 1 |
| 2025-06-01-backend-design-3.md | 20 | 2 |
| 2026-06-11-backend-design-3.5.md | 29 | 5 |
| 2026-06-18-backend-design-4.md | 31 | 3 |

## 2024-02-03-principled-pragmatic.md (29건)

### 🔴 High · 오타 — 158행: "떄로는 원인을 알 수 없는 버그로"

'떄로는'은 '때로는'의 오타다. 문단 첫 단어라서 눈에 잘 띈다.

**제안:** '때로는'으로 수정한다.

### 🔴 High · 오타 — 170행: "쉽게 원리주의적 접근을 선택기도 한다"

'선택기도 한다'는 '선택하기도 한다'에서 '하'가 누락된 오타로, 문장이 비문이 된다.

**제안:** '선택하기도 한다'로 수정한다.

### 🔴 High · 오타 — 258행: "명확하게 한 쪽에 책임과 관한을 부여하는 것이"

'관한'은 '권한'의 오타다. '책임과 권한'이라는 핵심 표현이 깨져서 독자가 잘못 읽을 수 있다.

**제안:** '책임과 권한을 부여하는 것이'로 수정한다.

### 🟡 Medium · 일관성 — 202행: "고압적인 마이크로 매니저와 같은 상황" (그 외 164, 166, 168, 170, 236행 등은 모두 '마이크로 관리자')

같은 개념을 글 전체에서 '마이크로 관리자'로 부르다가 202행에서만 '마이크로 매니저'로 표기해 용어가 오락가락한다.

**제안:** '마이크로 관리자'로 통일한다.

### 🟡 Medium · Front matter — 1-4행 front matter (layout, title만 존재). _layouts/base.html 2행: <html lang="{{ page.lang | default: site.lang | default: "en" }}">

모든 한국어 글(2024-02-03-principled-pragmatic.md, 2024-03-09-oop.md, 2024-05-04-ebi.md, backend-design 1~4·3.5)에 lang 필드가 없고 _config.yml에도 lang 설정이 없어, 레이아웃 기본값에 따라 한국어 페이지가 전부 <html lang="en">으로 렌더링된다. jekyll-seo-tag의 og:locale도 영어로 출력되어 검색엔진/스크린리더가 한국어 콘텐츠를 영어로 인식한다. 영문 번역본(-en.md)과의 구분 필드(lang, ref)도 전혀 없다.

**제안:** 한국어 글 front matter에 lang: ko를 추가한다(레이아웃이 page.lang을 이미 지원하므로 즉시 반영됨). 한국어 글이 다수이므로 _config.yml에 lang: ko를 기본값으로 두고 -en.md 글에만 lang: en을 지정하는 방식이 더 간단하다. 번역본 쌍 연결이 필요하면 ref 필드를 함께 도입한다.

### 🟡 Medium · 문법 — 224행: "원리주의 팀장이 쓸데없이 일을 벌린다고 생각한다"

'일을 벌리다'는 잘못된 표현이다. 일을 시작하거나 펼치는 것은 '벌이다'이고, '벌리다'는 간격을 넓히는 것이다(272행의 '틈을 크게 벌리는'은 올바른 용례).

**제안:** '일을 벌인다고 생각한다'로 수정한다.

### 🟡 Medium · 문법 — 118행: "테스트는 커녕 빌드만 해서 완료 보고를 하는 경우다"

'커녕'은 조사이므로 앞말에 붙여 써야 한다. '테스트는 커녕'은 띄어쓰기 오류다.

**제안:** '테스트는커녕'으로 붙여 쓴다.

### 🟡 Medium · 문법 — 30행: "신기술 도입을 가능한 피하려고 할 것이다"

관형사형 '가능한'은 부사어로 쓸 수 없다. '가능한 한(限)'이 올바른 표현이다.

**제안:** '가능한 한 피하려고 할 것이다'로 수정한다.

### 🟡 Medium · 문법 — 242행: "실용주의 팀장은 가능한 변화를 최소화 하거나 간단한 해결책을 선호하기 때문에"

'가능한 변화를 최소화'가 '가능한 한 변화를 최소화'인지 '가능한 변화(possible changes)를 최소화'인지 중의적으로 읽힌다. 문맥상 전자로 보인다.

**제안:** '가능한 한 변화를 최소화하거나'로 수정해 중의성을 없앤다.

### 🟡 Medium · 문법 — 28행: "성장에 도움이 되지 않는 일은 기술부채가 잔뜩 쌓인 코드를 유지보수 하거나 신규 개발이라도 오래되거나 대중적이지 않은 기술을 사용하는 프로젝트가 있다."

주어 '일은'과 서술어 '프로젝트가 있다'의 호응이 깨진 비문이다. 나열 구조도 꼬여서 읽기 어렵다.

**제안:** '성장에 도움이 되지 않는 일로는 기술부채가 잔뜩 쌓인 코드를 유지보수하는 일이나, 신규 개발이라도 오래되거나 대중적이지 않은 기술을 사용하는 프로젝트 등이 있다.'처럼 주술 호응을 맞춘다.

### 🟡 Medium · 문법 — 90행: "원리주의 팀장은 가장 많은 시행착오를 겪는 시기인데"

사람(팀장)을 '시기'라고 서술해 주술 호응이 깨졌다.

**제안:** '원리주의 팀장 시절은 가장 많은 시행착오를 겪는 시기인데' 또는 '원리주의 팀장은 가장 많은 시행착오를 겪는데'로 수정한다.

### 🟡 Medium · 문법 — 42행: "나는 잦은 이직을 하고 여러 팀장을 만나면서 내 뜻을 존중해 주는 팀장도 있었지만 그 중에서 두 번은 극단적인 실용주의 팀장이었다."

주어 '나는'이 뒤의 '팀장도 있었지만'과 호응하지 않는 비문이다. 또 사람을 '두 번'(횟수)으로 세고 있어 어색하다.

**제안:** '잦은 이직으로 여러 팀장을 만났는데, 내 뜻을 존중해 주는 팀장도 있었지만 그중 두 명은 극단적인 실용주의 팀장이었다.'처럼 수정한다.

### 🟡 Medium · 문법 — 60행: "그러나 관리자는 이런 변화나 문제에 쉽게 대응하지 못한다면 왜 설계에 많은 시간을 할애했는지 납득할 수 없을 것이다."

'대응하지 못한다면'의 주체는 원리주의 팀장인데 문장 구조상 '관리자'가 주어처럼 읽혀서 뜻이 잘못 전달될 수 있다.

**제안:** '그러나 팀장이 이런 변화나 문제에 쉽게 대응하지 못한다면, 관리자는 왜 설계에 많은 시간을 할애했는지 납득할 수 없을 것이다.'로 주어를 명시한다.

### 🟡 Medium · 문법 — 114행: "실용주의 팀원이 디자인 패턴을 공부한다면 그것이 궁금하기 보다는 면접에서 문제로 나올 수 있기 때문이다."

'공부한다면 ~ 때문이다' 구조로 조건절과 서술어가 호응하지 않는 비문이다.

**제안:** '실용주의 팀원이 디자인 패턴을 공부한다면 그것이 궁금해서라기보다는 면접에서 문제로 나올 수 있기 때문이다.'처럼 '궁금해서라기보다는'으로 고치거나 문장을 재구성한다.

### 🟡 Medium · 논리 — 8행 "양립할 수 없는 두 가지 이념", 202행 "원만한 협력 관계를 유지한다면, 이는 그들이 싸울 여력이 없을 정도로 큰 위협이 존재한다는 의미이다", 270행 "이념에 차이가 있어도 서로에 대한 신뢰만 충분하다면 서로를 존중하며 프로젝트를 잘 진행할 수 있을 것이다"

글 앞부분의 주장과 결론이 직접 충돌한다. 도입부(8행)에서는 두 이념을 '양립할 수 없는' 것으로 규정하고, 6장 도입부(202행)에서는 두 팀장의 원만한 협력은 외부의 큰 위협이 있을 때뿐이라고 단정한다. 그런데 결론(270행)에서는 신뢰만 충분하면 이념이 달라도 잘 협력할 수 있다고 정반대로 주장한다. 202행은 신뢰에 의한 협력 가능성을 명시적으로 배제하는 단정문이어서 결론과 양립하지 않는다.

**제안:** 202행의 단정을 완화하거나(예: "큰 위협이 존재하는 경우가 많다" 또는 "신뢰가 쌓이지 않은 상태라면" 같은 조건 추가), 도입부의 '양립할 수 없는'을 '자주 충돌하는' 정도로 조정해서 결론의 '신뢰가 있으면 협력 가능하다'는 메시지와 일관되게 맞춰야 한다.

### 🟡 Medium · 오타 — 276행: "실용주의 팀장들의 텃새를 버티기는 힘들 것이다"

'텃새'는 철새의 반대말(새의 종류)이고, 먼저 자리 잡은 사람의 위세를 뜻하는 말은 '텃세'다.

**제안:** '텃세를 버티기는'으로 수정한다.

### 🟡 Medium · 오타 — 44행: "개발 역량이 뒤쳐지는 것을 불안해 하는 와중에"

'뒤쳐지다'는 '뒤처지다'의 잘못된 표기다(표준어는 '뒤처지다').

**제안:** '뒤처지는'으로 수정한다.

### ⚪ Low · 일관성 — 18행 "### 1.1 실용주의 팀장과의 갈등" vs 52행 "### 2.1. 오버 엔지니어링", 206행 "### 6.1. 원리주의 팀장의 입장"

하위 제목 번호 표기가 1장에서는 '1.1, 1.2, 1.3'(마침표 없음), 2장과 6장에서는 '2.1., 6.1.'(마침표 있음)으로 일관되지 않다.

**제안:** 하위 제목 번호 끝 마침표 유무를 한 가지 방식으로 통일한다.

### ⚪ Low · 일관성 — 136행: "OOP/디자인패턴은 기본이고" (같은 행 앞부분과 44, 54, 114, 134행은 '디자인 패턴')

글 전체에서 '디자인 패턴'으로 띄어 쓰다가 이곳에서만 '디자인패턴'으로 붙여 써 표기가 일관되지 않다.

**제안:** 'OOP/디자인 패턴'으로 통일한다.

### ⚪ Low · 문법 — 28행 "유지보수 하거나", 32행 "유지보수 하면서", 50행 "계속 된다면", 100행 "합리화 하는", 120행 "심사숙고 하기를", 210행 "최소화 하는", 242행 "최소화 하거나", 284행 "최소화 해야"

'명사+하다/되다'가 한 단어로 굳어진 동사들('유지보수하다', '계속되다', '합리화하다', '심사숙고하다', '최소화하다')을 일관되게 띄어 쓰고 있다.

**제안:** 각각 '유지보수하거나', '유지보수하면서', '계속된다면', '합리화하는', '심사숙고하기를', '최소화하는/최소화하거나/최소화해야'로 붙여 쓴다.

### ⚪ Low · 문법 — 28행 "초조해 할 것이다", 44행 "불안해 하는", 110행 "힘들어 하는", 246행 "그럴듯 하다고"

'-어하다' 결합형('초조해하다', '불안해하다', '힘들어하다')과 한 단어인 '그럴듯하다'를 띄어 써서 맞춤법에 어긋난다.

**제안:** '초조해할', '불안해하는', '힘들어하는', '그럴듯하다고'로 붙여 쓴다.

### ⚪ Low · 문법 — 114행 "열정이라기 보다는", "궁금하기 보다는" / 256행 "상위 기술 책임자 뿐이다" / 272행 "벌리는 것 뿐이다" / 42행 "그 중에서"

조사 '보다', '뿐'과 합성어 '그중'을 앞말과 띄어 썼다. 182행에서는 '상황이라기보다는'으로 올바르게 붙여 써서 표기가 일관되지 않다.

**제안:** '열정이라기보다는', '궁금하기보다는', '책임자뿐이다', '것뿐이다', '그중에서'로 붙여 쓴다.

### ⚪ Low · 문법 — 250행: "프로젝트를 무사히 마치지 못 할 것이다"

'-지 못하다' 구성에서는 '못하다'를 붙여 써야 하므로 '마치지 못 할'은 띄어쓰기 오류다.

**제안:** '마치지 못할 것이다'로 수정한다.

### ⚪ Low · 문법 — 266행: "설계 권한과 책임은 아키텍트에 있었기 때문에"

사람(아키텍트)에게는 조사 '에'가 아니라 '에게'를 써야 한다.

**제안:** '아키텍트에게 있었기 때문에'로 수정한다.

### ⚪ Low · 문법 — 238행: "이런 상황은 이미 원리주의 팀장이 불리한 입장에서 시작하게 된다."

주어 '상황은'과 서술어 '시작하게 된다'의 호응이 어긋나 어색하다.

**제안:** '이런 상황에서는 원리주의 팀장이 이미 불리한 입장에서 시작하게 된다.'로 수정한다.

### ⚪ Low · 문체 — 264행: "이렇게 설계,구현,테스트로 조직이 나뉘어져 있었기 때문에"

쉼표 뒤에 띄어쓰기가 없고('설계,구현,테스트'), '나뉘어져'는 '나뉘다'에 '-어지다'가 겹친 이중 피동이다.

**제안:** '설계, 구현, 테스트로 조직이 나뉘어 있었기 때문에'로 수정한다.

### ⚪ Low · 문체 — 22행: "실용주의 팀장에 속한 원리주의 팀원은"

사람이 사람'에 속한'다는 표현이 어색하다. 팀에 속하거나 팀장 아래에 있는 것이다.

**제안:** '실용주의 팀장 밑에 있는 원리주의 팀원은' 또는 '실용주의 팀장의 팀에 속한 원리주의 팀원은'으로 수정한다.

### ⚪ Low · 문체 — 158행: "원인을 알 수 없는 버그로 많은 연구와 시행착오를 겪어야 할지도 모른다"

'연구를 겪다'는 호응하지 않는 연어 결합이다('시행착오를 겪다'만 자연스러움).

**제안:** '많은 연구와 시행착오가 필요할지도 모른다' 또는 '많은 조사를 하고 시행착오를 겪어야 할지도 모른다'로 수정한다.

### ⚪ Low · 오타 — 42행 "여러차례", 114행 "깊이있게", 218행 "안고있지만", 262행 "주목할만한", 32행 "되어있어야", 138행 "하고나면"

띄어 써야 할 곳을 붙여 쓴 띄어쓰기 오류들이다('여러 차례', '깊이 있게', 본용언+보조용언 '안고 있지만/되어 있어야/하고 나면', '주목할 만한').

**제안:** '여러 차례', '깊이 있게', '안고 있지만', '하고 나면'은 띄어 써야 한다. '주목할 만한', '되어 있어야'는 띄어 씀이 원칙이나 한글맞춤법 제47항에 따라 붙여 씀도 허용되므로 일관성 차원의 권장 수정이다.

## 2024-03-09-oop.md (31건)

### 🔴 High · 코드 — 328행 "reader.close();", 331~334행 인터페이스 선언, 336~348행 "class BufferDocument implements DocumentReadable"

글의 핵심인 객체 지향 예제 코드가 동작하지 않는다. DocumentReadable 인터페이스는 close(): void를 선언하고 printDocument()는 마지막에 reader.close()를 호출하는데, BufferDocument 클래스에는 close() 메서드가 없다. TypeScript에서는 'Property close is missing' 컴파일 에러(TS2420)가 나고, 타입 검사를 무시하고 실행해도 printDocument(bufferDocument) 호출 시 'reader.close is not a function' 런타임 에러가 발생한다. 절차식 코드의 문제를 해결했다고 제시하는 바로 그 코드가 깨져 있어 신뢰도에 치명적이다.

**제안:** BufferDocument에 close() 메서드를 추가한다(버퍼는 해제할 자원이 없으므로 `public close(): void {}` 같은 no-op으로 충분). 5절의 PlantUML 클래스 다이어그램(386~391행)에도 BufferDocument에 +close() : void를 함께 추가해 코드와 일치시킨다.

### 🟡 Medium · 코드 — 140~142행 "if(typeof doc === 'string') { doc = contents }", 152~153행 "doc = \"\""

updateDocument()와 clearDocument()에서 매개변수 doc에 재할당하는 코드는 실제로 아무 일도 하지 않는다. JS/TS에서 문자열은 값으로 전달되므로 함수 안에서 doc = contents, doc = ""를 해도 호출자(main의 buffer)에는 전혀 반영되지 않는다. 게다가 main()에서 buffer는 const로 선언(126행)되어 있어 의미상으로도 변경 불가능하다. '문서를 업데이트/초기화한다'는 본문 의도와 코드 동작이 일치하지 않는다.

**제안:** 버퍼 문서를 { contents: string } 같은 객체로 감싸서 전달해 실제로 변경이 반영되게 하거나, 절차식의 문제를 보여주기 위한 의사 코드임을 주석으로 명시한다(예: doc 재할당 대신 /* 버퍼 내용을 교체 */ 같은 주석 처리).

### 🟡 Medium · 코드 — 139행 "function updateDocument(doc: string | File, contents: string)", 151행, 163행 / 146~148, 158~160, 180~182행의 "else if(doc instanceof HttpRequest)"

세 번째 코드 블록의 updateDocument, clearDocument, printDocument는 매개변수 타입이 string | File로 선언되어 있는데 함수 본문에는 doc instanceof HttpRequest 분기가 있다. 바로 앞 코드 블록(92행)에서는 printDocument(doc: string | File | HttpRequest)로 HttpRequest를 타입에 포함했으므로 글 내에서 코드가 퇴행했고, 선언된 타입대로라면 HttpRequest 분기는 도달 불가능한 코드다.

**제안:** 세 함수의 시그니처를 모두 doc: string | File | HttpRequest로 수정해 앞 코드 블록과 일치시킨다. main()에서도 HttpRequest를 사용하는 예시(88~89행처럼)를 유지하면 '함수가 늘어날수록 수정 지점이 늘어난다'는 본문 주장과 코드가 정확히 맞아떨어진다.

### 🟡 Medium · 문법 — 435행: "왜냐하면 하나의 전역변수를 사용하는 모든 함수들은 서로 연결되는 것이다."

'왜냐하면'으로 시작한 문장이 '~기 때문이다'가 아니라 '~것이다'로 끝나서 호응이 깨진 비문이다.

**제안:** "왜냐하면 하나의 전역변수를 사용하는 모든 함수들은 서로 연결되기 때문이다."로 수정한다.

### 🟡 Medium · 문법 — 454행: "왜냐하면 두 프로퍼티(변수)가 비공개이기 때문에 외부에서의 접근이 원천적으로 차단되기 때문이다."

한 문장 안에 '때문에'와 '때문이다'가 중복되어 비문이 되었다.

**제안:** "왜냐하면 두 프로퍼티(변수)가 비공개여서 외부에서의 접근이 원천적으로 차단되기 때문이다."로 수정한다.

### 🟡 Medium · 문법 — 499행: "실제로 MSA를 적용한 프로젝트는 많지만 성공 사례가 적은 이유이다."

'~이유이다'의 주어가 없어서 문장이 성립하지 않는다. 앞 문장의 내용을 받는 주어가 빠졌다.

**제안:** "이것이 실제로 MSA를 적용한 프로젝트는 많지만 성공 사례는 적은 이유이다."처럼 주어 '이것이'를 추가한다.

### 🟡 Medium · 문체 — 493~494행: "그렇다면 데이터만 존재하는 객체가 있을 수 있을까? 반대로 함수만 존재하는 객체가 있을 수 있을까? 문법적으로 class를 사용해서 코드를 작성한다고 해도 그것은 객체가 아니다."

두 질문에 대한 답이 명시되지 않은 채 다음 문장으로 넘어가고, '그것'이 가리키는 대상(데이터만 있는 클래스인지, 함수만 있는 클래스인지, 둘 다인지)이 불명확해 뜻이 모호하다.

**제안:** "데이터만 있거나 함수만 있다면, 문법적으로 class 키워드를 사용해 작성했더라도 그것은 진정한 객체가 아니다."처럼 지시 대상을 명확히 밝힌다.

### 🟡 Medium · 기술 오류 — 491행 "OOP의 캡슐화, 은닉화, 다형성, 상속성이 무엇인지를 설명한다. OOP의 이 네 가지 기본 원칙은"

일반적으로 통용되는 OOP의 4대 특성은 캡슐화, 추상화, 상속, 다형성이다. 이 글은 추상화를 빼고 은닉화(정보 은닉)를 별도 항목으로 넣었는데, 정보 은닉은 보통 캡슐화의 한 측면으로 설명된다. '많은 글이나 영상이 설명하는 네 가지 기본 원칙'이라고 인용하는 맥락이므로 통용되는 목록과 다르면 독자에게 혼란을 준다.

**제안:** "캡슐화, 추상화, 다형성, 상속"으로 수정하거나, 은닉화를 굳이 언급하려면 "캡슐화(은닉화), 추상화, 다형성, 상속"처럼 캡슐화의 하위 개념으로 표기한다.

### ⚪ Low · 코드 — 292행 "function printDocument(doc: any, getChar: Func)"

Func는 TypeScript에 존재하지 않는 타입이며 글 어디에서도 정의되지 않는다. File, EOF 등은 가상의 도메인 객체로 볼 수 있지만 Func는 함수 타입 표기 자리이므로 TS 문법대로 쓰지 않으면 컴파일 에러가 나고, TS를 배우는 독자에게 잘못된 표기를 가르치게 된다.

**제안:** getChar: (doc: any) => string | null | undefined 처럼 함수 타입을 명시하거나, 최소한 type Func = (doc: any) => string | null 정의를 코드에 포함한다.

### ⚪ Low · 코드 — 351~355행 "private stream: ReadStream; ... this.stream = new File(filename);"

FileDocument에서 stream 프로퍼티의 타입은 ReadStream인데 생성자에서 new File(filename)을 할당한다. 글의 다른 예제에서 File은 getChar()/close()를 직접 제공하는 클래스로 사용되었으므로 ReadStream이라는 타입이 갑자기 등장할 이유가 없고, File이 ReadStream의 하위 타입이라는 설명도 없어 타입이 일치하지 않는다.

**제안:** private stream: File로 선언을 바꾸거나 new ReadStream(filename)으로 할당을 바꿔 선언과 생성 코드를 일치시킨다. ReadStream을 유지하려면 File과의 관계(예: File이 ReadStream을 구현)를 본문에서 한 줄 설명한다.

### ⚪ Low · 일관성 — 10·307·372·417·462행 "객체 지향" vs 496행 "객체지향으로", 501행 "객체지향 프로그래밍"

같은 용어를 글 앞부분에서는 '객체 지향'으로 띄어 쓰고 결론부에서는 '객체지향'으로 붙여 써서 표기가 오락가락한다.

**제안:** 글 전체에서 '객체 지향' 또는 '객체지향' 중 하나로 표기를 통일한다.

### ⚪ Low · 일관성 — 16~31행·269~301행(2칸 들여쓰기, 세미콜론 사용) vs 124~184행·232~255행(4칸 들여쓰기, 세미콜론 없음, else if 줄바꿈 스타일)

같은 글의 TypeScript 예제 코드인데 코드 블록마다 들여쓰기 폭, 세미콜론 유무, 중괄호 스타일이 달라 일관성이 없어 독자에게 혼란을 줄 수 있다.

**제안:** 모든 코드 블록을 동일한 포맷(예: 2칸 들여쓰기 + 세미콜론)으로 통일한다.

### ⚪ Low · 일관성 — 205~206행 "const file = new File(\"test.txt\"); printFileDocument(file);"

3.1절 예제에서만 file.close() 호출이 빠져 있다. 1.1절(46행), 2절(85행), 3.2절(289행) 예제는 모두 파일을 닫고, 134행에서는 "close 해야 한다는 규칙을 알아야 한다"고 강조까지 했으므로, 절차식에서 호출자가 자원 해제 규칙을 챙겨야 한다는 글의 논지와도 어긋나는 누락이다.

**제안:** 206행 다음에 file.close();를 추가해 다른 절차식 예제들과 일관성을 맞춘다.

### ⚪ Low · 문법 — 10행: "본 글에서는 ... OOP의 본질이 무엇인지 알아보고 사고의 전환을 이루는데 조금이나마 도움이 되려고 한다."

주어가 '본 글에서는'(부사어 형태)인데 서술어가 '도움이 되려고 한다'여서 주술 호응이 어긋난다. '~에서는'은 '되려고 한다'의 주어가 될 수 없다.

**제안:** "본 글은 ... OOP의 본질이 무엇인지 알아보고 사고의 전환을 이루는 데 조금이나마 도움이 되고자 한다."처럼 주어를 '본 글은'으로 바꾸거나, "...알아보면서 사고의 전환에 도움을 주고자 한다"로 다듬는다.

### ⚪ Low · 문법 — 188행: "실제 프로젝트는 이것보다 더 길고 복잡한 코드인 경우가 많다."

주어 '프로젝트'와 서술부 '코드인 경우가 많다'가 호응하지 않는다. 프로젝트가 코드일 수는 없다.

**제안:** "실제 프로젝트의 코드는 이것보다 더 길고 복잡한 경우가 많다."로 수정한다.

### ⚪ Low · 문법 — 263행: "근본 원인은 main() 함수에서 ... 그 데이터를 사용하는 방법은 전달하지 않기 때문이다."

'원인은 ~ 때문이다'는 주어('원인은')와 서술어('때문이다')가 의미상 중복되어 호응이 어긋나는 전형적인 비문이다.

**제안:** "근본 원인은 ... 방법은 전달하지 않는다는 데 있다." 또는 "printDocument()에서 if를 제거할 수 없는 것은 ... 전달하지 않기 때문이다."로 수정한다.

### ⚪ Low · 문법 — 370행: "코드에 타입을 판별하는 if 문이 보인다면 이것이 절차식은 아닌지? 객체 지향으로 개선할 수 없는지 고민해 보기를 바란다."

한 문장이 물음표로 중간에 끊겨 있어 어색하다. '아닌지'와 '없는지'가 모두 '고민해 보기를 바란다'에 걸리는 구조이므로 쉼표로 이어야 자연스럽다.

**제안:** "...if 문이 보인다면 절차식은 아닌지, 객체 지향으로 개선할 수 없는지 고민해 보기를 바란다."로 수정한다.

### ⚪ Low · 문법 — 497행: "좋은 코드가 무엇인지 많은 고민과 연습이 필요하다."

'무엇인지'와 '고민과 연습이 필요하다' 사이를 잇는 조사가 빠져 문장 성분 연결이 어색하다.

**제안:** "좋은 코드가 무엇인지에 대한 많은 고민과 연습이 필요하다."로 수정한다.

### ⚪ Low · 논리 — 320~329행 printDocument() 내부의 "reader.close();" 및 410~417행 '관심사 분리' 설명

객체 지향 버전에서 main()이 생성(자원 획득)한 객체를 printDocument()가 닫는다. 자원을 연 쪽이 닫는다는 일반적인 원칙에 어긋나고, 같은 객체를 두 번 출력할 수 없게 되며, '출력'이라는 함수가 '자원 해제'까지 담당해 바로 다음 절(5.1)에서 강조하는 관심사 분리와 긴장 관계에 있다. 이전 절차식 예제(85행, 289행)에서는 main()이 file.close()를 호출했으므로 close 책임이 이유 설명 없이 이동했다.

**제안:** main()에서 printDocument(fileDocument) 호출 후 fileDocument.close()를 호출하도록 바꾸고 printDocument()에서는 reader.close()를 제거한다. 또는 printDocument()가 닫도록 의도한 것이라면 그 설계 결정(호출자가 수명 관리를 위임)을 본문에서 한 줄이라도 설명한다.

### ⚪ Low · 오타 — 10행: "사고의 전환을 이루는데 조금이나마 도움이 되려고 한다"

'데'가 장소·경우를 뜻하는 의존명사이므로 띄어 써야 하는데 붙여 썼다.

**제안:** "사고의 전환을 이루는 데 조금이나마"로 띄어쓰기를 수정한다.

### ⚪ Low · 오타 — 74행: "main()함수는 물론이고 printDocument() 함수도"

'main()함수'는 붙여 쓰고 바로 뒤의 'printDocument() 함수'는 띄어 써서 같은 문장 안에서 표기가 어긋난다.

**제안:** "main() 함수는 물론이고"로 띄어 써서 통일한다.

### ⚪ Low · 오타 — 122행 "그 만큼 늘어난다", 423행 "그 만큼 더 강하게 결합하게 된다"

'그만큼'은 한 단어이므로 붙여 써야 한다. 두 곳에서 모두 '그 만큼'으로 잘못 띄어 썼다.

**제안:** 두 곳 모두 "그만큼"으로 붙여 쓴다.

### ⚪ Low · 오타 — 186행: "if-else로 함수 마다 타입 체크를 하고 있다."

'마다'는 조사이므로 앞말에 붙여 써야 한다.

**제안:** "함수마다"로 붙여 쓴다.

### ⚪ Low · 오타 — 198행 "세분화 하는", 490행 "그룹화 한다는 것이", 491행 "그룹화 할 때의", 496행 "구체화 되지는"

'-화'가 붙은 명사 뒤의 '하다/되다'는 접미사이므로 붙여 써야 한다(세분화하다, 그룹화하다, 구체화되다).

**제안:** 각각 "세분화하는", "그룹화한다는", "그룹화할 때의", "구체화되지는"으로 붙여 쓴다.

### ⚪ Low · 오타 — 257행: "이럴거면 차라리 if를 사용하는 것이 낫다."

'거'(것)는 의존명사이므로 띄어 써야 한다.

**제안:** "이럴 거면"으로 띄어 쓴다.

### ⚪ Low · 오타 — 303행: "printDocument()는 if 문이 필요없고"

'필요 없다'는 표준 띄어쓰기상 띄어 써야 한다.

**제안:** "if 문이 필요 없고"로 수정한다.

### ⚪ Low · 오타 — 415행 "`DocumentReadable` 인터페이스 뿐이고", 419행 "코드 뿐만이 아니라"

체언 뒤의 '뿐'은 조사이므로 앞말에 붙여 써야 한다.

**제안:** "인터페이스뿐이고", "코드뿐만이 아니라"로 붙여 쓴다.

### ⚪ Low · 오타 — 417행: "인터페이스와 구현을 분리하는 매커니즘은"

외래어 표기법상 'mechanism'의 올바른 표기는 '메커니즘'이다.

**제안:** "메커니즘"으로 수정한다.

### ⚪ Low · 오타 — 456행: "잠재적으로 전역변수와 다를바 없게 되는 것이다."

'바'는 의존명사이므로 띄어 써야 한다.

**제안:** "다를 바 없게"로 띄어 쓴다.

### ⚪ Low · 오타 — 496행: "지금 이 글을 읽고 뭔가를 알것 같은 느낌을"

'것'은 의존명사이므로 띄어 써야 한다.

**제안:** "알 것 같은"으로 띄어 쓴다.

### ⚪ Low · 오타 — 499행: "OOP를 깊이있게 이해하지 못하면"

'있다'는 본용언이므로 '깊이 있게'로 띄어 써야 한다.

**제안:** "깊이 있게"로 띄어 쓴다.

## 2024-05-04-ebi.md (26건)

### 🔴 High · 논리 — 73행: "어느날 사용자는 방향키의 모양을 *[그림 2-4]* 처럼 변경해 달라고 가볍게 요구할 수 있다. ... 그러나 개발자는 구현 방법을 바꿔야 하는 큰 일이 된다."

그림 참조가 잘못되어 논증이 자기모순이 된다. [그림 2-4]는 "무언가를 가리키는 용도의 화살표"로, 같은 모양의 화살표를 회전시킨 형태다. 61행에서 이미 "[그림 2-4]처럼 ... 용도라면 방법#1처럼 화살표를 회전시켜서 구현하는 것이 맞다"고 했으므로, 회전 방식(방법#1)으로 구현한 개발자에게 [그림 2-4]처럼 바꾸는 요청은 이미지 한 장만 교체하면 되는 쉬운 작업이다. 즉 "구현 방법을 바꿔야 하는 큰 일"이라는 주장과 충돌한다. 문맥상 키마다 서로 다른 모양을 가지는 [그림 2-5]를 가리켜야 회전 기반 구현이 깨져서 주장이 성립한다.

**제안:** 73행의 "*[그림 2-4]*"를 "*[그림 2-5]*"로 수정한다(서로 다른 모양의 화살표를 가지는 방향키로 변경 요청을 받아야 방법#1 구현을 갈아엎어야 한다는 논지가 성립함).

### 🔴 High · 오타 — 224행: "영사확인 절차를 보다 보다 간소화 한 것이다"

'보다'가 두 번 연속으로 중복 입력된 명백한 오타다. 또한 '간소화 한'은 '간소화한'으로 붙여 써야 한다.

**제안:** "영사확인 절차를 보다 간소화한 것이다"로 수정

### 🟡 Medium · 코드 — 90행: "아래처럼 `Up, Down, Left, Right` 클래스로 정의해서" (코드는 94-97행 `const Up = () => <Image .../>`)

본문은 '클래스로 정의'한다고 설명하지만 바로 아래 코드는 클래스가 아니라 화살표 함수(함수형 컴포넌트)로 정의하고 있어 설명과 코드가 일치하지 않는다.

**제안:** "`Up, Down, Left, Right` 컴포넌트로 정의해서" 등 코드와 일치하는 표현으로 수정

### 🟡 Medium · 코드 — 364-365행: "%0A%0A%ED%85%8C%EC%8A%A4%ED%8A%B8.jpg", "%0A%0A%ED%8C%8C%EC%9D%BC.json"

인코딩 예시 앞에 붙은 '%0A%0A'는 개행 문자 두 개를 인코딩한 것으로, 디코딩하면 '\n\n테스트.jpg'가 되어 아래의 원본 파일명 예시('테스트.jpg', '파일.json')와 일치하지 않는다. 복사 과정에서 들어간 잔재로 보인다.

**제안:** '%ED%85%8C%EC%8A%A4%ED%8A%B8.jpg', '%ED%8C%8C%EC%9D%BC.json'으로 '%0A%0A' 제거

### 🟡 Medium · 일관성 — 185행 제목 "상속(Inheritance)과 합성(Composition)" vs 219행 "이것은 포함으로 표현하는 것이 자연스럽다."

같은 개념(Composition)을 섹션 제목과 189행에서는 '합성'으로, 219행에서는 '포함'으로 다르게 불러 독자가 다른 개념으로 오해할 수 있다.

**제안:** 219행을 "이것은 합성으로 표현하는 것이 자연스럽다."로 통일하거나 '포함(합성)'으로 병기

### 🟡 Medium · 문법 — 149행 "표현하는데 어려움이 있다", 166행 "표현하는데 적합하다", 387행 "인지하는데 도움이"

의존명사 '데'는 띄어 써야 한다('~하는 데'). 84행에서는 "선택하는 데 있어서"로 바르게 띄어 써서 문서 내 표기도 일관되지 않다.

**제안:** '표현하는 데 어려움이 있다', '표현하는 데 적합하다', '인지하는 데 도움이'로 수정

### 🟡 Medium · 문법 — 10행: "왜냐하면 두 방법 모두 장단점이 명확했기 때문에 어느 한 방법을 선택해도 아쉬움이 남았기 때문이었다."

'왜냐하면 ~때문에 ~때문이었다'로 이유 표현이 한 문장에 세 번 겹쳐 비문이 됐다.

**제안:** "두 방법 모두 장단점이 명확해서 어느 한쪽을 선택해도 아쉬움이 남았기 때문이다."처럼 이유 표현을 한 번만 사용

### 🟡 Medium · 문법 — 73행: "그러나 개발자는 구현 방법을 바꿔야 하는 큰 일이 된다."

주어 '개발자는'과 서술어 '큰 일이 된다'의 호응이 깨졌다(개발자가 큰일이 되는 것이 아님). 또한 '중대한 일'의 의미로는 '큰일'로 붙여 쓴다.

**제안:** "그러나 개발자에게는 구현 방법을 바꿔야 하는 큰일이 된다."로 수정

### 🟡 Medium · 문법 — 247행: "나는 영사확인과 아포스티유가 비슷해 보이는 것은 우연일 뿐이며 동일한 문서로 취급하면 안 될 것처럼 보였다."

주어 '나는'과 서술어 '보였다'의 호응이 맞지 않는 비문이다.

**제안:** "나에게는 ... 안 될 것처럼 보였다" 또는 "나는 ... 안 된다고 판단했다"로 수정

### 🟡 Medium · 문법 — 172행: "두 방식 중에서 무엇을 선택할 것인지는 개념적인 관점에서 영화 예매 프로세스를 더 잘 표현하는 것이 무엇인지를 봐야 한다."

주어부 '무엇을 선택할 것인지는'과 서술어 '봐야 한다'가 호응하지 않는 비문이다.

**제안:** "두 방식 중 무엇을 선택할지는 ... 더 잘 표현하는 것이 무엇인지에 달려 있다." 또는 "두 방식 중에서 선택하려면 ... 무엇인지를 봐야 한다."로 수정

### 🟡 Medium · 문법 — 180행: "중요한 것은 요구사항을 보다 정확히 반영하는 것이 무엇이냐인 것이다."

'무엇이냐인 것이다'는 서술어가 꼬인 어색한 비문이다.

**제안:** "중요한 것은 무엇이 요구사항을 보다 정확히 반영하느냐다."처럼 정리

### 🟡 Medium · 문법 — 79-80행: "여러 방법을 두고 고민하게 되는 이유 중에 하나는 당연한 정보는 누락하기 때문이다."

'이유 중에 하나는 ~때문이다'로 호응이 어긋나고, '당연한 정보는 누락하기'는 조사와 태가 맞지 않아(누락하는 주체 불명) 어색하다.

**제안:** "여러 방법을 두고 고민하게 되는 이유 중 하나는 당연하다고 여기는 정보가 누락되기 때문이다."로 수정

### 🟡 Medium · 기술 오류 — 355행: "그러니까 HTML의 전송 과정에서 발생하는 기술적인 문제는 그 과정에서 해결해야 하는 것이지"

URL 인코딩은 HTML이 아니라 URI/HTTP 전송 계층의 제약(RFC 3986의 ASCII 제한)에서 비롯되는 문제다. HTML은 마크업 언어이지 전송 프로토콜이 아니므로 "HTML의 전송 과정"이라는 표현은 기술적으로 부정확하며, 같은 단락 앞부분에서 "URL Encoding은 ASCII 문자 집합의 제한 때문"이라고 올바르게 설명한 것과도 어긋난다.

**제안:** "HTML의 전송 과정"을 "HTTP 전송 과정" 또는 "URL 전송 과정"으로 수정한다.

### 🟡 Medium · 오타 — 73행 "최적화 하는", 90행 "변경 되더라도", 109행 "디자인 해야", 136·153행 "디자인 한다면", 226행 "암호화 하고", 280행 "구체화 되기", 282행 "확장 되면서", 345행 "업로드 하려고", 347행 "업로드 하는", 353행 "업로드 한"

'명사+하다/되다' 형태의 파생 동사를 일관되게 띄어 쓰고 있다. 맞춤법 규정상 '최적화하다', '디자인하다', '암호화하다', '구체화되다', '확장되다', '업로드하다' 등은 한 단어로 붙여 써야 한다.

**제안:** 각각 '최적화하는', '변경되더라도', '디자인해야', '디자인한다면', '암호화하고', '구체화되기', '확장되면서', '업로드하려고/하는/한'으로 붙여 쓰기

### 🟡 Medium · 오타 — 18행 "이 때 방향키를", 73행 "그리고 어느날 사용자는", 75행 "어려워 지게 된다", 86행 "스스로 채워넣어야 한다", 377행 "있을 수 밖에 없고", 387행 "알고보면 당연한"

띄어쓰기 오류 모음: '이 때'는 한 단어 '이때'로, '어느날'은 '어느 날'로, '어려워 지게'는 '어려워지게'로, '채워넣어야'는 '채워 넣어야'로, '수 밖에'는 조사 '밖에'를 붙여 '수밖에'로, '알고보면'은 '알고 보면'으로 써야 한다.

**제안:** '이때', '어느 날', '어려워지게 된다', '채워 넣어야 한다', '있을 수밖에 없고', '알고 보면'으로 수정

### ⚪ Low · 코드 — 361행: "```ts" 코드 블록 (362-371행)

코드 블록 언어가 `ts`(TypeScript)로 지정되어 있으나 내용은 코드가 아니라 파일명 목록이다. `%ED%95%9C%EA%B8%80.txt` 같은 줄은 TypeScript 문법이 아니므로 구문 강조가 잘못 적용될 수 있다.

**제안:** 언어 태그를 `text`(또는 태그 없음)로 변경한다.

### ⚪ Low · 일관성 — 172행 "영화 예매 프로세스" vs 174-177행 "티켓 구매 프로세스"

같은 단락에서 동일한 프로세스를 '영화 예매 프로세스'와 '티켓 구매 프로세스'라는 두 이름으로 부르고 있다. 109행 이후 줄곧 '영화 예매'였으므로 갑자기 '티켓 구매'가 등장하면 혼란스럽다.

**제안:** '영화 예매 프로세스'로 용어 통일

### ⚪ Low · 일관성 — 347행 "URL encoding을 해서" vs 355행 "애초에 URL Encoding은"

'URL encoding'과 'URL Encoding'으로 대소문자 표기가 일관되지 않다.

**제안:** 'URL 인코딩' 또는 'URL encoding' 한 가지로 통일

### ⚪ Low · 일관성 — 20행 "![방향키1](/assets/images/virtual-gamepad.jpg)" / 36행 "![방향키1](/assets/images/arrows1.svg)"

서로 다른 그림(그림 2-1과 그림 2-2)의 대체 텍스트(alt)가 둘 다 '방향키1'로 중복되어 있다. 이후 그림은 방향키2~4로 번호가 이어져 표기가 어긋난다.

**제안:** 20행의 alt를 '게임패드' 등 그림 내용에 맞게 바꾸거나 그림 번호에 맞춰 alt 텍스트를 재정렬

### ⚪ Low · 문법 — 71행: "사용자에게 보여지는 결과물은 같기 때문에"

'보여지는'은 이중 피동으로 어문 규범상 지양해야 하는 표현이다.

**제안:** "사용자에게 보이는 결과물은"으로 수정

### ⚪ Low · 문법 — 351행 "`%ED%95%9C%EA%B8%80.txt`으로 저장", 353행 "`한글.txt`으로 저장", 357행 "`한글.txt`으로 생각"·"`%ED%95%9C%EA%B8%80.txt`으로 한다면" (반면 349행은 "`한글.txt`로 저장")

받침 없이 끝나는 'txt(티엑스티/텍스트)' 뒤에는 조사 '로'를 써야 하는데 '으로'를 썼고, 349행에서는 '로'를 써서 같은 문서 안에서 표기가 오락가락한다.

**제안:** 모두 '~.txt`로'로 통일

### ⚪ Low · 논리 — 377행: "이것은 설계도 마찬가지인데 설계 단계에서 설계자의 모든 생각을 정리할 수 없다. 모든 요구사항과 설계를 최대한 반영한 것이 코드이기 때문이다."

'~때문이다'가 앞 문장(모든 생각을 정리할 수 없다)의 근거 역할을 하지 못해 인과 연결이 어색하고 의미가 모호하게 전달된다.

**제안:** "코드는 이렇게 빈틈이 있는 요구사항과 설계를 최대한 반영해야 하는 결과물이다."처럼 인과 관계를 풀어 쓰거나 접속 표현을 조정

### ⚪ Low · 구조 — 379행 "`본질 기반 해석(EBI)`의 중요한 역할" vs 381행 "`본질 기반 해석(Essence-Based Interpretation, EBI)`"

약어 'EBI'가 379행에서 먼저 사용되고 정식 영문 명칭은 381행에서야 소개되어 순서가 뒤바뀌었다.

**제안:** 첫 등장인 379행에서 '본질 기반 해석(Essence-Based Interpretation, EBI)'으로 전체 명칭을 소개하고 이후 약어 사용

### ⚪ Low · 문체 — 8행: "나는 좋은 코드에 대해서 많은 노력을 해왔다."

'~에 대해서 노력을 해왔다'는 호응이 어색한 표현이고, 바로 다음 문장("좋은 코드를 작성하기 위해서...")과 내용이 겹친다.

**제안:** "나는 좋은 코드를 작성하기 위해 많은 노력을 해왔다." 또는 "나는 좋은 코드에 대해 오랫동안 고민해 왔다."로 수정

### ⚪ Low · 문체 — 347행: "사용자가 파일을 다운로드 받으려면"

'다운로드 받다'는 의미가 중복된 겹말이다(download = 내려받다).

**제안:** "파일을 다운로드하려면" 또는 "파일을 내려받으려면"으로 수정

### ⚪ Low · 기술 오류 — 347행: "사용자가 업로드 하는 파일명에 특수문자가 포함되어 있어서 URL encoding을 해서 서버에 전송해야 한다."

예시 파일명 `한글.txt`에는 특수문자가 없다. 인코딩이 필요한 이유는 특수문자가 아니라 비ASCII 문자(한글)이기 때문이며, 355행에서 스스로 "URL Encoding은 ASCII 문자 집합의 제한 때문"이라고 설명한 것과도 어긋난다.

**제안:** "파일명에 비ASCII 문자(한글)가 포함되어 있어서" 등으로 수정하여 예시(한글.txt) 및 355행의 설명과 일치시킨다.

## 2025-04-01-backend-design-1.md (14건)

### 🔴 High · 문법 — 304행: "여기서 주목할 것은 상영 시간을 생성하면 티켓도 같이 생성해야 하는 것을 알 수 있다."

주술 호응이 깨진 비문이다. 주어부 '주목할 것은'에 대응하는 서술어가 '~알 수 있다'로 끝나서 문장 구조가 맞지 않는다.

**제안:** "여기서 주목할 것은 상영 시간을 생성하면 티켓도 같이 생성해야 한다는 점이다." 또는 "여기서 상영 시간을 생성하면 티켓도 같이 생성해야 한다는 것을 알 수 있다."로 수정

### 🟡 Medium · 코드 — 447-448행: "admin ..> admin1", "admin ..> admin2"

5.4절 PlantUML 다이어그램에서 `admin`이라는 요소가 선언되지 않은 채 관계 정의에 사용되었다. 선언된 액터는 admin1(Call Center Agent), admin2(Ticket Checker)뿐이어서 렌더링 시 의도하지 않은 'admin' 요소가 생성되거나 다이어그램이 의도와 다르게 표시된다.

**제안:** `actor administrator as admin`을 선언에 추가하거나, 해당 두 줄(admin ..> admin1, admin ..> admin2)을 삭제

### 🟡 Medium · 문법 — 163행 "최소화 하려고", 262행 "세분화 해보자", 429행 "세분화 할 수 있을지도", 452행 "세분화 한다면"

'명사+하다' 파생 동사의 띄어쓰기 오류가 반복된다. '최소화하다', '세분화하다'는 한 단어이므로 붙여 써야 한다.

**제안:** 각각 "최소화하려고", "세분화해 보자", "세분화할 수 있을지도", "세분화한다면"으로 수정

### 🟡 Medium · 문법 — 428행: "어디부터 설명을 해야할지 난감해 할 수도 있다"

띄어쓰기 오류 두 건이 한 문장에 있다. '해야 할지'는 띄어 써야 하고, '난감해하다'는 '형용사+어하다' 구성으로 한 단어이므로 붙여 써야 한다.

**제안:** "어디부터 설명을 해야 할지 난감해할 수도 있다"로 수정

### 🟡 Medium · 문법 — 429행: "외부 서비스가 있을 지도 모른다"

'-ㄹ지'는 어미이므로 붙여 써야 한다. 같은 문장 바로 앞의 "세분화 할 수 있을지도 모른다"에서는 올바르게 붙여 써서 표기가 오락가락한다.

**제안:** "외부 서비스가 있을지도 모른다"로 수정

### ⚪ Low · 일관성 — 194, 202, 227, 304행 "상영 시간" vs 243, 284, 333, 399행 "상영시간"

같은 개념인데 본문에서는 '상영 시간', 다이어그램과 목록에서는 '상영시간'으로 띄어쓰기 표기가 혼용된다.

**제안:** 글 전체에서 '상영 시간' 또는 '상영시간' 중 하나로 통일

### ⚪ Low · 일관성 — 113, 120, 141행 "Payment Gateway" vs 268, 293, 304, 315행 등 "PaymentGateway"

같은 외부 시스템을 4장에서는 'Payment Gateway'(띄어쓰기), 5장 이후 본문과 다이어그램에서는 'PaymentGateway'(붙여쓰기)로 표기가 혼용된다.

**제안:** 본문과 다이어그램의 표기를 둘 중 하나로 통일 (예: 다이어그램 식별자는 PaymentGateway, 표시 이름은 "Payment Gateway"로 일관되게 정의)

### ⚪ Low · 일관성 — 293-294행 "PurchaseTickets --> PaymentGateway" vs 355-356, 419-420행 "PurchaseTickets ..> PaymentGateway"

동일한 관계가 5.2절 다이어그램에서는 실선 화살표(-->)로, 5.3절 두 다이어그램에서는 점선 화살표(..>)로 그려져 다이어그램 간 표기가 일치하지 않는다.

**제안:** 같은 관계는 같은 화살표 표기로 통일 (예: 모두 ..> 사용)

### ⚪ Low · 일관성 — 90행 "`customer`와 `administrator`" vs 237행 "administrator는", 308행 "customer와 administrator의"

같은 용어(customer, administrator)에 백틱(코드 서식)을 붙이는 곳과 붙이지 않는 곳이 섞여 있어 렌더링 시 표기가 일관되지 않는다.

**제안:** 본문에서 액터 이름을 언급할 때 백틱 사용 여부를 한 가지로 통일

### ⚪ Low · 문법 — 186행: "제공해야 하는 기능일테니까 말이다"

'터'는 의존명사이므로 '기능일 테니까'로 띄어 써야 한다.

**제안:** "기능일 테니까 말이다"로 수정

### ⚪ Low · 문법 — 159행: "의존 관계를 표현하는데 있어서"

'데'가 '~하는 것에'를 뜻하는 의존명사로 쓰였으므로 띄어 써야 한다.

**제안:** "표현하는 데 있어서"로 수정 (또는 간결하게 "표현할 때")

### ⚪ Low · 문법 — 46행: "그 때 가서 다시 검토해야 하는 문제다"

'그때'는 한 단어이므로 붙여 써야 한다.

**제안:** "그때 가서 다시 검토해야 하는 문제다"로 수정

### ⚪ Low · 문체 — 41행: "극장은 최소 4,000개 이상"

'최소'와 '이상'이 의미가 겹치는 군더더기 표현이다.

**제안:** "극장은 4,000개 이상" 또는 "극장은 최소 4,000개"로 수정

### ⚪ Low · 문체 — 231행: "어떤 액션을 주고받는지를 정리하다 보면 자연스럽게 정리가 되는 것이다"

'정리하다'와 '정리가 되다'가 한 문장에서 반복되어 어색하고, 무엇이 정리된다는 것인지(유스케이스 구조) 모호하다.

**제안:** "어떤 액션을 주고받는지 따져 보면 유스케이스가 자연스럽게 정리된다."처럼 반복을 피하고 대상을 명시

## 2025-05-01-backend-design-2.md (30건)

### 🔴 High · 구조 — 332행: "그래서 자연스럽게 **`/showtime-creation/` 와 같은 네임스페이스를 떠올릴 수 있다."

굵게(**) 마크다운이 열리기만 하고 닫히지 않아 렌더링 시 별표가 그대로 노출되거나 이후 텍스트 서식이 깨진다.

**제안:** 강조를 의도했다면 "**`/showtime-creation/`과 같은 네임스페이스**"처럼 닫는 **를 추가하고, 강조가 불필요하면 여는 **를 삭제한다.

### 🟡 Medium · 일관성 — 413행 "마이크로서비스 아키텍쳐(MSA) 소개", 415행 "최상위 아키텍쳐", 653행 "최상위 아키텍처로"

같은 글 안에서 '아키텍쳐'와 '아키텍처' 표기가 혼용된다. 외래어 표기법상 표준 표기는 '아키텍처'다.

**제안:** 413행 1곳과 415행 2곳('최상위 아키텍쳐', '마이크로서비스 아키텍쳐')의 '아키텍쳐'를 모두 '아키텍처'로 수정한다.

### 🟡 Medium · 일관성 — 87-88행: "- 영화의 상영시간이 성공적으로 등록되어야 합니다. / - 상영시간에 해당하는 티켓이 생성되어야 한다."

유스케이스 명세서의 '사후 조건' 목록 안에서 첫 항목은 '합니다'체, 둘째 항목은 '한다'체로 문체가 어긋난다. 명세서 전체(60-87행)가 경어체인데 88행만 평어체다.

**제안:** 88행을 "상영시간에 해당하는 티켓이 생성되어야 합니다."로 수정한다.

### 🟡 Medium · 일관성 — 8행: "두 유스케이스인 `상영시간 생성하기`와 `티켓 구매하기`에 대해서 좀 더 분석해 보도록 하겠다." / 655행: "다음 글에서는 `상영시간 생성하기`의 부족했던 설계를 마무리 보완하고"

도입부에서 이번 글에서 두 유스케이스를 분석하겠다고 예고했지만, 본문 전체는 `상영시간 생성하기`만 다루고 `티켓 구매하기`는 한 번도 분석하지 않는다. 결론에서도 다음 글 역시 `상영시간 생성하기` 보완이라고 밝히고 있어, 도입부의 약속이 글 안에서 지켜지지 않는다.

**제안:** 도입부를 "이번 시간에는 그중 `상영시간 생성하기`를 먼저 분석한다. `티켓 구매하기`는 이후 글에서 다룬다"로 수정하거나, 결론에 `티켓 구매하기` 분석 시점을 명시한다.

### 🟡 Medium · 일관성 — 88행: "상영시간에 해당하는 티켓이 생성되어야 한다." / 555행: "[ShowtimeCreationService] --> [TicketsService]" / 585~642행(5.4절 최종 시퀀스 다이어그램)

유스케이스 명세서의 사후 조건과 도입부 유스케이스 다이어그램(CreateShowtimes ..> GenerateTickets), 그리고 SoLA 패키지 다이어그램의 TicketsService 의존 화살표까지 모두 티켓 생성을 요구하는데, 5.2절과 5.4절의 상영시간 생성 시퀀스 다이어그램 어디에도 TicketsService 호출(티켓 생성)이 없다. 이 누락이 의도적이라는 언급도 본문에 없어(결론의 "부족했던 설계"는 무엇이 부족한지 특정하지 않음) 설계가 자신의 사후 조건을 충족하지 못하는 것으로 읽힌다. 참고로 다음 글(3편)에서야 "티켓을 생성하는 부분도 빠져있다"고 명시된다.

**제안:** 5.4절 또는 결론에 "티켓 생성과 상영시간 충돌 검증은 다음 글에서 보완한다"는 문장을 명시적으로 추가하여, 최종 다이어그램에서 티켓 생성이 빠진 것이 의도된 단계적 전개임을 밝힌다.

### 🟡 Medium · 일관성 — 8행 "절차가 복잡해 보이는 두 유스케이스인 `상영시간 생성하기`와 `티켓 구매하기`에 대해서 좀 더 분석해 보도록 하겠다."

2편 서두에서 `상영시간 생성하기`와 `티켓 구매하기` 두 유스케이스를 분석하겠다고 예고했지만, 2편은 물론 3, 3.5, 4편 어디에서도 `티켓 구매하기` 분석은 등장하지 않는다. 3.5편 복습(84행)에서도 "`상영시간 생성` 케이스를 시퀀스 다이어그램으로 정리했다"며 한 개만 다룬 것으로 서술하고, 2편에서 노란색으로 강조했던 PurchaseTickets(19행)도 3.5편 복습 다이어그램에서는 강조가 사라져 서로 어긋난다.

**제안:** 2편 서두를 "두 유스케이스 중 먼저 `상영시간 생성하기`를 분석한다" 정도로 수정하거나, `티켓 구매하기` 분석을 다룰 회차를 명시한다(예: "`티켓 구매하기`는 이후 편에서 다룬다").

### 🟡 Medium · 일관성 — 655행 "다음 글에서는 `상영시간 생성하기`의 부족했던 설계를 마무리 보완하고 관련 테스트를 작성할 것이다."

2편 결론은 다음 글(3편)에서 설계 보완과 함께 "관련 테스트를 작성할 것"이라고 예고했지만, 3편에는 테스트 내용이 전혀 없고 3편 결론(808행)에서 다시 "다음 시간에는 테스트에 대해서 이야기 하겠다"고 미룬다. 테스트는 실제로 4편에서야 다뤄진다.

**제안:** 2편 결론에서 테스트 언급을 제거하고 "부족했던 설계를 보완할 것이다"까지만 예고하거나, "테스트는 그 이후 편에서 다룬다"로 수정한다.

### 🟡 Medium · 논리 — 533행: "MSA는 ‘서비스 간 협력’과 ‘순환 참조 금지’라는 상충을 레이어 분리로 해결한다."

5.3절에서 'MSA는 순환 참조 문제가 발생할 수 있다'고 한 직후, SoLA를 소개하는 5.4절 첫 문장의 주어가 다시 'MSA'여서 해결 주체가 모호하다. 문맥상 레이어 분리로 해결하는 것은 SoLA다. 또한 '~라는 상충을 해결한다'는 명사 사용이 어색하다.

**제안:** "SoLA는 ‘서비스 간 협력’과 ‘순환 참조 금지’ 사이의 상충 문제를 레이어 분리로 해결한다."처럼 주어를 SoLA로 바꾸고 '상충 문제'로 표현을 다듬는다.

### 🟡 Medium · 문체 — 193행 "그러나 프레임이 중첩되니 분석 단계의 문서임에도 불구하고 복잡해 보인다." / 197행 "프레임이 중첩되니까 분석 단계의 문서임에도 불구하고 복잡해 보인다."

거의 동일한 문장이 연속된 두 문단(3.2 마지막과 3.3 첫 문장)에서 반복되어 글이 중복되고 퇴고가 덜 된 인상을 준다.

**제안:** 둘 중 하나를 삭제하거나, 197행을 "앞서 본 것처럼 프레임이 중첩되면 복잡해 보이므로" 정도로 앞 문장을 받아서 요약하는 형태로 고친다.

### 🟡 Medium · 기술 오류 — 492행: "객체 지향 프로그래밍(OOP)에서 두 객체가 서로를 참조하면 메모리 관리 등의 문제가 생기기 때문에 피해야 한다고 한다."

순환 참조가 메모리 관리 문제를 일으킨다는 설명은 참조 카운팅 기반 환경(C++ shared_ptr, Swift ARC, 구형 IE의 COM 등)에만 해당한다. 이 시리즈가 다루는 NestJS/TypeScript를 포함해 추적형 GC(mark-and-sweep)를 쓰는 Java, C#, JavaScript 등에서는 순환 참조 자체가 메모리 누수를 일으키지 않는다. 일반적인 OOP의 사실처럼 서술하면 독자가 잘못된 지식을 얻을 수 있다.

**제안:** "참조 카운팅 방식의 환경에서는 메모리 누수 문제가 생길 수 있다"로 한정하거나, 메모리 언급을 빼고 글의 핵심 논지인 강한 결합 문제만 근거로 제시한다. (NestJS의 경우 순환 의존 시 DI가 실패해 forwardRef가 필요해지는 문제를 예로 드는 것이 더 정확하다.)

### ⚪ Low · 코드 — 474행 "Gateway -> ShowtimesService: createShowtimes(createDto)" 및 481행 "ShowtimesService -> ShowtimesService: createShowtimes(createDto)"

5.2절 시퀀스 다이어그램에서 Gateway가 호출한 createShowtimes() 내부의 자기 호출(self-call)에 동일한 이름 createShowtimes(createDto)를 그대로 사용해, 코드로 읽으면 무한 재귀처럼 보인다. 실제 의도는 검증 후의 내부 저장(생성) 단계로 추정되나 다이어그램만으로는 구분되지 않는다.

**제안:** 내부 자기 호출의 메시지명을 saveShowtimes(createDto) 등 외부 진입 메서드와 다른 이름으로 바꾸어 의도를 명확히 한다.

### ⚪ Low · 일관성 — 330행 "**‘상영 시간 생성’**과 같은 상위 개념", 332행 "**‘상영 시간 생성’이라는 도메인 엔티티/유스케이스를 기점으로**"

글 전체에서 '상영시간'으로 붙여 쓰다가 4.3절에서만 '상영 시간'으로 띄어 써서 표기가 오락가락한다.

**제안:** '상영시간 생성'으로 통일한다.

### ⚪ Low · 일관성 — 529행: "언젠가 상호 참조 관계로 발전할 수 있다는 뜻이다."

해당 절(5.3) 전체에서 '순환 참조'라는 용어를 쓰다가 이 문장에서만 '상호 참조'로 바뀌어 같은 개념인지 독자가 혼동할 수 있다.

**제안:** '순환 참조 관계로 발전할 수 있다'로 용어를 통일한다.

### ⚪ Low · 일관성 — 571-579행: "사용자 시나리오를 조립합니다 ... 외부 시스템 연동을 담당합니다."

본문 전체가 평어체('-다')인데 5.4절의 계층 설명 목록만 경어체('-합니다')로 작성되어 문체가 혼용된다. 2절의 유스케이스 명세서는 인용 문서로 볼 수 있으나, 이 목록은 본문 설명에 해당한다.

**제안:** 해당 목록을 "사용자 시나리오를 조립한다", "Core/Infra 서비스 호출만 허용한다" 등 평어체로 통일한다.

### ⚪ Low · 일관성 — 56행 "**액터**: Admin", 98행 "actor Admin"

1편과 2편의 유스케이스 다이어그램(2편 14, 37행 포함)에서는 액터를 `administrator`로 명명했는데, 2편의 유스케이스 명세서와 시퀀스 다이어그램부터는 같은 액터가 `Admin`으로 표기되어 시리즈 내 액터 명칭이 두 가지로 혼용된다.

**제안:** 시리즈 전체에서 `administrator` 또는 `Admin` 중 하나로 통일하거나, 처음 `Admin`을 쓰는 곳에 "이하 administrator를 Admin으로 줄여 쓴다"는 안내를 추가한다.

### ⚪ Low · 일관성 — 421-435행 ditaa 다이어그램 "| - Property |<------>| - Database |"

Class와 Microservice를 대응시키는 동일한 ditaa 다이어그램이 2024-03-09-oop.md(464-478행)에서는 `+ Property` / `+ Database`로, 2편에서는 `- Property` / `- Database`로 가시성 기호가 다르게 표기되어 있다. 내부 구현(DB)이 외부에 노출되지 않는다는 두 글의 공통 설명에 맞는 표기는 `-`(private) 쪽이므로, 같은 그림이 글마다 다르면 독자가 의도를 헷갈릴 수 있다.

**제안:** oop.md의 다이어그램도 `- Property` / `- Database`로 수정해 두 글의 그림을 일치시킨다.

### ⚪ Low · 문법 — 655행: "부족했던 설계를 마무리 보완하고 관련 테스트를 작성할 것이다."

'마무리 보완하고'는 명사와 동사가 어색하게 결합된 비문에 가까운 표현이다.

**제안:** "부족했던 설계를 마저 보완하고" 또는 "부족했던 설계를 보완해 마무리하고"로 수정한다.

### ⚪ Low · 링크 — 334행 "본질 기반 해석(EBI) 관점에서는 **Top-Down**이 ...", 3.5편 492행 "(본질기반해석이란 얘기다)"

backend-design 시리즈에서 2024-05-04-ebi.md에서 정의한 `본질 기반 해석(EBI)` 개념을 두 차례 인용하지만, 해당 글로 연결되는 링크가 없다. 시리즈만 읽는 독자는 EBI가 무엇인지 알 수 없다. 또한 3.5편은 띄어쓰기 없이 "본질기반해석"으로 표기해 용어 표기도 EBI 글의 "본질 기반 해석(EBI)"과 다르다.

**제안:** EBI를 처음 언급하는 2편 4.3절과 3.5편에 EBI 글(/2024/05/04/ebi 등 실제 퍼머링크)로의 링크를 추가하고, 표기를 "본질 기반 해석(EBI)"으로 통일한다.

### ⚪ Low · 링크 — 6행 "우리는 지난 시간에 `Movie Booking System`의 유스케이스 다이어그램을 그렸다." (3편 6행, 3.5편 449행, 4편 6행도 동일 패턴)

시리즈 글들이 "지난 시간에", "1,2,3편에서" 등으로 이전 글을 지칭하지만, 시리즈 어느 글에도 이전·다음 글로 연결되는 실제 하이퍼링크가 없다. 검색 등으로 중간 회차에 유입된 독자는 앞뒤 글을 찾기 어렵다.

**제안:** 각 글 서두나 말미에 시리즈 목차 또는 이전/다음 글 링크를 추가한다(예: "이전 글: 백엔드 서비스 분석과 설계 (1)" 링크).

### ⚪ Low · 문체 — 46행: "조회 기능은 전제 데이터가 있어야 의미를 파악할 수 있기 때문이다."

'전제 데이터'라는 표현이 어색해서 의미가 한 번에 와닿지 않는다. 또한 '조회 기능은 ... 의미를 파악할 수 있기'의 주술 호응도 불분명하다(의미를 파악하는 주체는 기능이 아니라 사람).

**제안:** "조회 기능은 데이터가 먼저 있어야 의미가 있기 때문이다." 또는 "조회 기능은 전제가 되는 데이터가 있어야 동작을 확인할 수 있기 때문이다."처럼 다듬는다.

### ⚪ Low · 문체 — 334행: "본질 기반 해석(EBI) 관점에서는"

'EBI'라는 약어가 글에서 처음 등장하는데 원어 풀이나 설명이 없어 '본질 기반 해석'과 어떻게 대응되는지 독자가 알 수 없다.

**제안:** 약어가 글에서 다시 쓰이지 않으므로 '(EBI)'를 삭제하고 '본질 기반 해석 관점에서는'으로만 쓰거나, 약어를 유지하려면 영문 원어를 병기해 통용 약어(Entity-Boundary-Interactor)와의 혼동을 막는다.

### ⚪ Low · 기술 오류 — 332행: "Top-Down(개념 우선) 접근은 **‘상영 시간 생성’이라는 도메인 엔티티/유스케이스를 기점으로**"

'상영시간 생성'은 행위(유스케이스)이지 도메인 엔티티가 아니다. DDD 용어로 엔티티는 식별자를 가진 도메인 객체를 가리키므로 '도메인 엔티티/유스케이스'라는 표현은 개념을 혼동시킨다.

**제안:** "‘상영시간 생성’이라는 유스케이스(도메인 개념)를 기점으로"와 같이 '엔티티'라는 단어를 제거한다.

### ⚪ Low · 오타 — 332행: "`/showtime-creation/` 와 같은"

조사 '와'가 앞말과 띄어 쓰여 있다(조사는 앞말에 붙여 써야 함). 또한 여기만 끝에 슬래시가 붙은 `/showtime-creation/` 표기로, 본문 다른 곳(300행, 644행)의 `/showtime-creation`과 표기가 다르다.

**제안:** "`/showtime-creation`과 같은"으로 수정해 조사를 붙이고 슬래시 표기를 본문과 통일한다.

### ⚪ Low · 오타 — 120행: "내용을 담고있고 `대안 흐름`은 빠져있다."

'-고 있다' 구성의 보조용언은 붙여 쓸 수 없으므로 '담고있고'는 띄어쓰기 오류다.

**제안:** '담고 있고'로 수정한다.

### ⚪ Low · 오타 — 197행: "무엇이 옳은지는 프로젝트 마다 다르지만"

'마다'는 조사이므로 앞말에 붙여 써야 한다.

**제안:** '프로젝트마다'로 수정한다.

### ⚪ Low · 오타 — 289행: "그 만큼 사용과 구현이 어려워진다는 뜻이다."

부사로 쓰인 '그만큼'은 한 단어이므로 '그 만큼'은 띄어쓰기 오류다.

**제안:** '그만큼'으로 수정한다.

### ⚪ Low · 오타 — 291행: "명확한 목적을 가진 요청이라는 것을 알고있는 상황이다."

'-고 있다' 구성의 보조용언은 붙여 쓸 수 없으므로 '알고있는'은 띄어쓰기 오류다.

**제안:** '알고 있는'으로 수정한다.

### ⚪ Low · 오타 — 417행: "이 때 중요한 점은"

'이때'는 한 단어로 굳어진 합성어이므로 붙여 써야 한다.

**제안:** '이때'로 수정한다.

### ⚪ Low · 오타 — 441행: "편의를 위해서 `Admin`액터는 생략한다."

코드 스팬 `Admin`과 '액터' 사이에 띄어쓰기가 빠졌다.

**제안:** "`Admin` 액터는"으로 띄어 쓴다.

### ⚪ Low · 오타 — 494행: "예를 들면 클래스A, B가 서로를 참조한다고 할 때"

'클래스'와 'A' 사이에 띄어쓰기가 빠졌다.

**제안:** "클래스 A, B가"로 수정한다.

## 2025-06-01-backend-design-3.md (20건)

### 🔴 High · 코드 — 241행 "for(timeslot = startTime; timeslot <= endTime; timeslot+=10)" / 219행 "Set<number> timeslots = [0930,0940,0950,1200,1210,1220]"

수도코드의 타임슬롯 등록 루프가 `<=`로 endTime 슬롯까지 포함하는데, 4.1절의 예시 결과(219행)는 endTime 슬롯을 제외한다(0930+30분이면 `<=` 기준으로는 끝 시각 슬롯도 포함되어야 함). 코드와 예시가 모순된다. 또한 `<=`로 endTime을 등록하면, 신규 상영이 10:00에 끝나고 기존 상영이 10:00에 시작하는 정상적인 연속 편성을 충돌로 오판하는 false positive가 발생한다(검사 루프는 기존 상영의 startTime부터 확인하므로).

**제안:** 등록 루프를 반열림 구간 기준인 `timeslot < endTime`으로 수정해서 예시 결과와 일치시키고, 연속 편성(끝 시각 == 시작 시각)이 충돌로 판정되지 않게 해야 한다.

### 🔴 High · 코드 — 352~355행 "(showtime.startTime <= startTime && startTime <= showtime.endTime) || (showtime.startTime <= endTime && endTime <= showtime.endTime)"

4.5절의 대안 코드가 '느릴 뿐(O(M*N)) 동작은 한다'는 전제로 제시되어 있지만, 이 겹침 판정은 신규 상영시간이 기존 상영시간을 완전히 포함하는 경우를 놓친다. 예: 신규 09:00~13:00, 기존 10:00~11:00이면 startTime(0900)도 endTime(1300)도 [1000,1100] 구간에 들어가지 않아 충돌이 검출되지 않는다. 이는 false negative라서 글이 최우선 요구사항이라고 강조한 '좌석 중복 예약'을 그대로 유발할 수 있는 잘못된 코드다.

**제안:** 표준 구간 겹침 판정인 `startTime < showtime.endTime && showtime.startTime < endTime`으로 수정하거나, 본문에 이 코드가 포함 관계를 놓치는 불완전한 코드라는 점을 명시해야 한다.

### 🟡 Medium · 일관성 — 464행 "createTicketCreateDto(seat, showtime.id)" 및 466행 "createTickets(ticketCreateDtos,transactionId)" vs 403행 "buildCreateTicketDto(seat, showtime.id)" 및 405행 "createTickets(createTicketDtos,transactionId)" (704·706행도 build/createTicketDtos 사용)

5장과 7.4절 다이어그램에서는 'buildCreateTicketDto'/'createTicketDtos'인데, 같은 내용을 합쳤다는 6장 전체 시퀀스 다이어그램에서만 'createTicketCreateDto'/'ticketCreateDtos'로 이름이 달라 같은 함수/DTO가 다른 이름으로 보인다.

**제안:** 464행을 'buildCreateTicketDto(seat, showtime.id)', 466행을 'createTickets(createTicketDtos,transactionId)'로 통일한다.

### 🟡 Medium · 일관성 — 212행 "\"durationInMinutes\": 30", 557행 "\"durationInMinutes\": 90" (BulkCreateShowtimesDto)

2편에서는 같은 엔드포인트(POST /showtime-creation/showtimes)의 DTO 필드를 `durationInSeconds`로 정의했는데(2편 268행, 399-401행 CreateShowtimesDto), 3편에서 아무 설명 없이 `durationInMinutes`로 바뀌었다. DTO 이름 변경(CreateShowtimesDto → BulkCreateShowtimesDto)은 7.1 네이밍 절에서 설명되지만 시간 단위 변경은 언급이 없다. 4편 curl 예시는 다시 `"duration":120`(437, 477행)으로 또 달라진다.

**제안:** 시리즈 전체에서 상영시간 길이 필드를 한 가지(예: durationInMinutes)로 통일하고, 2편의 다이어그램을 수정하거나 3편에서 단위를 변경한 이유를 한 줄 덧붙인다. 4편의 단순화 예시도 같은 필드명을 쓰는 것이 혼란을 줄인다.

### 🟡 Medium · 문법 — 151행 "`Ticket`는", 184행 "`ShowtimeCreationService`은", 579행 "`BulkCreateShowtimesDto`을 받는다", 612행 "`ShowtimeCreationWorkerService`은", 614행 "`ShowtimeCreationStatus.Waiting`나 `ShowtimeCreationStatus.Processing`는", 733행 "`transactionId`을 이용한"

영어 용어 뒤 조사가 발음 기준 받침 유무와 맞지 않는 곳이 여러 군데 있다. 'Ticket(티켓)'은 받침으로 끝나므로 '은', 'Service(서비스)'는 모음으로 끝나므로 '는', 'Dto(디티오)'와 'transactionId(아이디)'는 모음으로 끝나므로 '를', 'Waiting/Processing(~잉)'은 받침으로 끝나므로 '이나/은'이 맞다.

**제안:** 각각 'Ticket은'(151행), 'ShowtimeCreationService는'(184행), 'BulkCreateShowtimesDto를'(579행), 'ShowtimeCreationWorkerService는'(612행), 'Waiting이나 / Processing은'(614행), 'transactionId를'(733행)로 수정한다.

### 🟡 Medium · 문법 — 337행 "한 가지 확실한 것은 설계를 하는 것은 그것이 효율적이기 때문이어야 한다."

주어('확실한 것은')와 서술어('때문이어야 한다')의 호응이 깨진 비문이다. '~것은'이 두 번 겹치고 문장이 '~는 것이다'로 끝나지 않아 의미 파악이 어렵다.

**제안:** '한 가지 확실한 것은, 설계를 하는 이유는 그것이 효율적이기 때문이어야 한다는 것이다.' 정도로 수정한다.

### 🟡 Medium · 문법 — 263행 "그러나 중첩된 루프는 입력에 비례하는 것이 아니라 duration 만큼 반복되는 거의 고정된 값이다."

주어 '중첩된 루프는'과 서술어 '고정된 값이다'의 호응이 맞지 않는다(루프 자체가 '값'일 수 없음). 말하려는 것은 안쪽 루프의 반복 횟수다.

**제안:** '그러나 안쪽 루프의 반복 횟수는 입력 크기에 비례하지 않고 duration에 따라 정해지는 거의 고정된 값이다.'처럼 주어를 '반복 횟수'로 바꾼다.

### 🟡 Medium · 문법 — 805행 "`ChatGPT-O3`와 `Gemini 2.5Pro`에 MSA를 반영한 엔티티 설계를 요청하면 `Seat`를 엔티티로 설계했다."

조건절 '요청하면'(현재/가정)과 주절 '설계했다'(과거)의 시제가 호응하지 않는 비문이다.

**제안:** '요청했더니 Seat를 엔티티로 설계했다' 또는 '요청하면 Seat를 엔티티로 설계한다'로 시제를 통일한다.

### 🟡 Medium · 논리 — 228행 "`1100,1110,1120,1130,1140,1150,1200,1210,1220,1230` 이렇게 상영시간을 10분 단위로 쪼개서" / 253행 "timeslot < endTime"

본문의 슬롯 나열이 수도코드와 두 가지로 불일치한다. (1) 나열은 1150 다음 1200으로 건너뛰지만(시간 인지 연산), 수도코드는 정수 `+=10` 연산이라 1160,1170,1180,1190이 생성된다. HHMM 숫자 인코딩에 정수 덧셈을 하면 시(時) 경계에서 값이 어긋나 실제 충돌을 놓칠 수 있다(예: 10:50+40분을 1090으로 계산하면 11:10 시작 상영과의 겹침을 검출 못 함). (2) 나열은 endTime인 1230을 포함하지만 검사 루프는 `< endTime`이라 1220에서 멈춘다.

**제안:** 타임슬롯을 HHMM 문자열 형태의 숫자가 아니라 epoch 분 단위나 Date 기반 연산으로 표현하도록 수도코드를 고치고, 본문 나열도 검사 루프의 경계 조건(`< endTime`)과 일치하게 1230을 제외하도록 수정해야 한다.

### 🟡 Medium · 논리 — 203행 "기존에 존재하는 showtimes의 `startTime`과 `endTime`이 timeslots에 존재하는지 확인한다." (634~636행 노트에도 동일 문구 반복)

알고리즘 설명이 기존 상영시간의 startTime과 endTime '두 끝점만' timeslots에 있는지 확인하는 것처럼 쓰여 있다. 끝점만 확인하면 신규 상영시간이 기존 상영시간 내부에 완전히 포함되는 충돌(예: 기존 09:00~14:00, 신규 10:00~10:30)을 놓친다. 실제 수도코드(253행)는 startTime부터 endTime까지 모든 10분 슬롯을 순회하므로 설명과 코드가 일치하지 않는다.

**제안:** "기존 showtimes의 startTime부터 endTime까지를 10분 단위 슬롯으로 쪼개어 각 슬롯이 timeslots에 존재하는지 확인한다"로 설명을 수정해야 한다(7.3절 다이어그램 노트도 동일하게 수정).

### ⚪ Low · 코드 — 219행 "Set<number> timeslots = [0930,0940,0950,1200,1210,1220]"

ts 코드블록으로 표기되어 있지만 유효한 TypeScript가 아니다. `Set<number> timeslots = ...`는 Java식 선언이고(TS는 `const timeslots: Set<number>`), `0930` 같은 선행 0 숫자 리터럴은 TS에서 문법 오류이며, Set에 배열 리터럴을 직접 대입할 수도 없다.

**제안:** `const timeslots = new Set<number>([930, 940, 950, 1200, 1210, 1220])` 형태로 고치거나, 의사 표기임을 드러내도록 코드블록 언어를 ts가 아닌 txt로 바꾸는 것이 좋다.

### ⚪ Low · 일관성 — 47행 "상영 시간을 등록한다고", 222행 "기존에 등록된 상영 시간은", 266-267행 "새로 추가할 상영 시간 수" vs 6행·56행·200행 등 "상영시간"

같은 글에서 핵심 용어가 '상영시간'(붙임)과 '상영 시간'(띄움) 두 가지 표기로 혼용되고 있다.

**제안:** 글 전체에서 우세하게 쓰인 '상영시간'으로 표기를 통일한다(47, 222, 266, 267행 수정).

### ⚪ Low · 일관성 — 241행 "timeslots.set(timeslot)" / 254행 "timeslots.exists(timeslot)" vs 284행 "timeslots.add(timeslot)" / 294행 "timeslots.contains(timeslot)"; 250행 "for (showtime in showtimes)" vs 348행 "for (showtime of showtimes)"

동일한 연산이 수도코드에서는 set/exists, 액티비티 다이어그램에서는 add/contains로 다르게 표기되어 있고, 루프 키워드도 in/of가 혼용되어 독자가 다른 동작으로 오해할 수 있다.

**제안:** 수도코드와 다이어그램의 메서드 이름을 add/contains(또는 set/exists) 한쪽으로 통일하고, 루프 키워드도 'of'로 통일한다.

### ⚪ Low · 일관성 — 123행 "### 2.1 `Movie` 엔티티" 등 2장 소제목(마침표 없음) vs 198행 "### 4.1. 충돌 검사 알고리즘", 523행 "### 7.1." 등(마침표 있음)

소제목 번호 표기가 '2.1'처럼 마침표 없는 형식과 '4.1.'처럼 마침표로 끝나는 형식이 혼용되어 있다.

**제안:** 소제목 번호 형식을 '4.1.' 또는 '2.1' 중 한 가지로 글 전체에서 통일한다.

### ⚪ Low · 일관성 — 740~744행 "class ShowtimeCreationService{ searchMovies() searchTheaters() requestShowtimeCreation(createDto) }" (483행과 비교)

8절 전체 서비스 요약의 ShowtimeCreationService 클래스에 `searchShowtimes()`가 빠져 있다. 483행에서 "searchMovies, searchTheaters, searchShowtimes 함수도 ShowtimeCreationService에서 구현해야 한다"고 명시했고 7.1절 시퀀스 다이어그램에도 searchShowtimes 호출이 있으므로 요약 다이어그램과 본문이 불일치한다.

**제안:** 8절 요약 다이어그램의 ShowtimeCreationService에 `searchShowtimes(searchDto)`를 추가해야 한다.

### ⚪ Low · 구조 — 309행 "### 4.4.현실적인 validateRequest 함수 설계"

다른 소제목(예: 198행 "### 4.1. 충돌 검사 알고리즘")과 달리 번호 뒤 띄어쓰기가 빠져 '4.4.현실적인'으로 붙어 렌더링된다.

**제안:** '### 4.4. 현실적인 validateRequest 함수 설계'로 번호 뒤에 공백을 추가한다.

### ⚪ Low · 오타 — 127행 "단순화 했다", 801행 "리팩토링 했다", 808행 "이야기 하겠다"

'명사+하다'가 한 단어로 굳어진 파생어는 붙여 써야 하는데 띄어 쓰여 있다.

**제안:** 각각 '단순화했다', '리팩토링했다', '이야기하겠다'로 붙여 쓴다.

### ⚪ Low · 오타 — 54행 "등록할 때 마다", 141행 "각 좌석 마다", 263행 "duration 만큼", 362행 "timeslots 방식 보다"

'마다', '만큼', '보다'는 체언 뒤에서 조사이므로 앞말에 붙여 써야 하는데 띄어 쓰여 있다.

**제안:** 각각 '때마다'(54행), '좌석마다'(141행), 'duration만큼'(263행), '방식보다'(362행)로 붙여 쓴다.

### ⚪ Low · 오타 — 151행 "모두 갖고있다", 184행 "추적하는데 사용된다", 333행 "자세히 해야하는 걸까", 614행 "검은점에 연결된다"

기타 띄어쓰기 오류. '-고 있다' 구성의 보조 용언은 붙여 쓸 수 없고, '추적하는 데'의 '데'는 의존 명사이며, '해야 하는'과 '검은 점'도 띄어 써야 한다.

**제안:** 각각 '갖고 있다'(151행), '추적하는 데'(184행), '해야 하는'(333행), '검은 점'(614행)으로 수정한다.

### ⚪ Low · 오타 — 805행 "`ChatGPT-O3`와 `Gemini 2.5Pro`에"

제품명 표기 오류. OpenAI 모델은 소문자 'o3'로 표기하며 'ChatGPT-O3'라는 공식 명칭은 없고, 'Gemini 2.5Pro'는 '2.5'와 'Pro' 사이 공백이 빠졌다.

**제안:** 'ChatGPT o3'(또는 'OpenAI o3')와 'Gemini 2.5 Pro'로 표기를 수정한다.

## 2026-06-11-backend-design-3.5.md (29건)

### 🔴 High · 코드 — 657행 `const {readFileSync, readFileAsync} = require("fs")`, 666행 `readFileAsyncText("./example.txt", (text) => {`

코드 예제가 실제로 동작하지 않는다. (1) Node.js의 fs 모듈에는 `readFileAsync`라는 함수가 존재하지 않는다(비동기 버전은 `readFile` 또는 `fs.promises.readFile`). (2) import한 이름은 `readFileAsync`인데 호출하는 함수는 `readFileAsyncText`로, 어디에도 정의되지 않아 실행하면 ReferenceError가 발생한다. (3) 실제 `fs.readFile`의 콜백 시그니처는 `(err, data)`이지 `(text)`가 아니다.

**제안:** `const { readFileSync, readFile } = require("fs")`로 수정하고, 호출부를 `readFile("./example.txt", "utf8", (err, text) => { asyncText = text })`로 통일한다. 665행 주석의 함수명(`readFileAsyncText()`)도 같이 맞춘다.

### 🔴 High · 일관성 — 238-242행 "creation -> creation: sagaId / creation -> queue: enqueue { createDto, sagaId }", 449행 "여기까지 1,2,3편에서 다뤘던 내용이다." (그 외 300, 350-356, 371-380, 419-441행)

3편(2025-06-01)에서는 작업 추적 식별자를 일관되게 `transactionId`로 명명했는데(3편 172-184, 432-434, 511, 563-568, 714-733행), 3.5편은 "여기까지 1,2,3편에서 다뤘던 내용이다"라며 이전 글의 다이어그램을 그대로 복습한다고 하면서 동일한 다이어그램의 용어를 모두 `sagaId`로 바꿔 놓았다. 용어 변경에 대한 설명이 전혀 없어 3편을 읽고 온 독자는 다른 개념으로 오해할 수 있다.

**제안:** 3.5편 서두나 해당 다이어그램 직전에 "3편의 transactionId를 Saga 패턴 도입을 고려해 sagaId로 이름을 바꿨다"는 한 줄 설명을 추가하거나, 3편을 sagaId로 통일 수정한다. 4편도 sagaId를 사용하므로(407, 420, 640행) 시리즈 전체에서 한 용어로 통일하고 변경 시점을 명시해야 한다.

### 🔴 High · 문법 — 491행 "상영시간이 존재하면 티켓이 존재해야 하고 티켓이 존재한다는 것은 상영시간이 존재해야 한다."

뒤 절의 주술 호응이 깨진 비문. 주어 '티켓이 존재한다는 것은'에 서술어 '상영시간이 존재해야 한다'가 호응하지 않음. 글의 핵심 주장을 담은 문장이라 더 문제가 됨.

**제안:** "상영시간이 존재하면 티켓이 존재해야 하고, 티켓이 존재한다는 것은 상영시간이 존재한다는 뜻이다." 또는 "~티켓이 존재하려면 상영시간이 존재해야 한다."로 수정.

### 🔴 High · 문체 — 8행 "설계를 해왔습니다", 28행 "세분화 됐습니다" vs 10행 "시작했다", 84행 "정리했다"

글 도입부(8행, 28행)는 경어체(~습니다)인데 10행부터 끝까지 나머지 본문은 모두 평어체(~했다, ~한다)여서 문체가 혼용됨. 같은 단락 흐름 안에서 어미가 오락가락해 독자에게 미완성 원고라는 인상을 줌.

**제안:** 글 전체를 평어체로 통일: "설계를 해왔습니다" → "설계를 해왔다", "세분화 됐습니다" → "세분화됐다". (또는 전체를 경어체로 통일)

### 🔴 High · 기술 오류 — 400-404행 note: "1. 생성하려는 상영시간을 10분 단위의 timeslots(Set)으로 등록한다. 2. 기존에 존재하는 showtimes의 `startTime`과 `endTime`이 timeslots에 존재하는지 확인한다."

설명된 충돌 검사 알고리즘이 올바르지 않다. (1) 기존 상영시간이 새 상영시간 구간을 완전히 포함하는 경우(예: 기존 08:00–12:00, 신규 09:00–10:30) 기존의 startTime/endTime 둘 다 timeslots 밖이라 겹침을 탐지하지 못한다. (2) 기존 상영의 endTime이 새 상영의 startTime과 정확히 일치하는 연속 상영(기존 08:00–09:00, 신규 09:00–)은 충돌이 아닌데 endTime이 Set에 들어 있어 오탐된다. (3) 시간이 10분 경계에 정렬되지 않으면(예: 95분 상영) Set 멤버십 검사 자체가 어긋난다.

**제안:** Set 멤버십 검사 대신 구간 겹침 검사(`existing.startTime < new.endTime && new.startTime < existing.endTime`)로 기술하거나, 기존 showtime의 전체 구간을 슬롯으로 전개해 교집합을 확인하는 방식으로 note를 수정한다.

### 🟡 Medium · 코드 — 657행 "const {readFileSync, readFileAsync} = require(\"fs\")" vs 666행 "readFileAsyncText(\"./example.txt\", ...)"

코드 예제에서 구조 분해로 가져온 이름은 readFileAsync인데 실제 호출하는 함수는 readFileAsyncText로 이름이 서로 다름(665행 주석도 readFileAsyncText). 예제를 따라 읽는 독자에게 정의되지 않은 함수를 호출하는 것으로 보임.

**제안:** 임포트와 호출 이름을 통일하되 fs에 존재하는 실제 API를 쓴다: `const { readFileSync, readFile } = require("fs")`로 수정하고 호출부를 `readFile("./example.txt", "utf8", (err, text) => { asyncText = text })`로, 665행 주석의 함수명도 함께 맞춘다.

### 🟡 Medium · 일관성 — 453행·538행·542행·570행 "ShowtimeCreator", "TicketCreator" vs 299행 "ShowtimeBulkCreatorService", 519행 "TicketBulkCreatorService"

본문에서는 ShowtimeCreator/TicketCreator라고 부르는데 다이어그램의 실제 클래스명은 ShowtimeBulkCreatorService/TicketBulkCreatorService임. 282행에서는 또 'Worker, Creator, Validator'로 지칭함. 같은 컴포넌트가 글 안에서 세 가지 이름으로 불려 독자가 매칭하기 어려움.

**제안:** 본문 표기를 다이어그램의 클래스명과 통일하거나, 282행에서 "이하 ShowtimeBulkCreatorService는 ShowtimeCreator로 줄여 부른다"처럼 약칭임을 한 번 명시.

### 🟡 Medium · 일관성 — 84행 "`상영시간 생성` 케이스를 시퀀스 다이어그램으로 정리했다." (직후 86~118행 다이어그램)

문장은 '시퀀스 다이어그램으로 정리했다'고 하는데 바로 아래에 오는 다이어그램은 시퀀스 다이어그램이 아니라 해당 유스케이스를 강조한 유스케이스 다이어그램임. 실제 시퀀스 다이어그램은 120행 이후에 나와서 글과 그림이 어긋남.

**제안:** "이 유스케이스 중 비교적 복잡해 보이는 `상영시간 생성` 케이스(아래 노란색 표시)를 시퀀스 다이어그램으로 정리하기로 했다."처럼 문장을 다이어그램에 맞게 수정.

### 🟡 Medium · 일관성 — 344행 `"durationInMinutes": 90` vs 155행·207행 `durationInSeconds`

같은 상영시간 생성 요청 DTO인데 앞의 두 시퀀스 다이어그램(155, 207행)에서는 `durationInSeconds`, 리팩토링 후 다이어그램(344행)에서는 `durationInMinutes`로 필드 단위가 설명 없이 바뀐다. 독자가 DTO를 추적하면 혼란스럽다.

**제안:** 세 다이어그램의 필드명을 하나로 통일하거나(예: 모두 `durationInMinutes`), 단위를 변경한 이유를 본문에 한 줄 언급한다.

### 🟡 Medium · 문법 — 453행 "여기서 ShowtimeCreator가 티켓까지 생성하는 것은 서비스 레벨의 단일 책임 원칙을 어기는 것이 아닌지 의아해 할지도 모르겠다."

주어 '~생성하는 것은'과 서술어 '의아해 할지도 모르겠다'의 호응이 어긋남(의아해하는 주체는 독자임). 또한 '의아해하다'는 한 단어이므로 '의아해 할지도'는 띄어쓰기 오류.

**제안:** "여기서 ShowtimeCreator가 티켓까지 생성하는 것이 서비스 레벨의 단일 책임 원칙을 어기는 것은 아닌지 의아해할지도 모르겠다."로 수정.

### 🟡 Medium · 논리 — 492-493행 "개념적으로 강하게 결합된 도메인을 설계와 구현에 그대로 반영하는 것이 최우선적으로 지켜야 할 원칙이다" vs 495행 "개발자 본능을 거스를 수 없다면 리팩토링을 할 수 있다", 538행 "고쳐놓고 보니 이게 더 적절해 보인다"

앞에서는 ShowtimeCreator가 티켓까지 생성하는 설계를 '최우선 원칙'(본질기반해석)으로 정당화하고 리팩토링을 원칙에 어긋나는 '개발자 본능'에 대한 양보로 소개해 놓고, 바로 뒤에서 분리한 설계가 "더 적절해 보인다"고 평가한다. 최우선 원칙을 따른 설계보다 그에 반한다고 소개한 설계가 더 적절하다는 것이어서 앞뒤 주장이 충돌한다.

**제안:** 분리 후에도 Worker가 두 Creator를 같은 유스케이스 안에서 동기적으로 묶고 있으므로 본질기반해석을 위반하지 않는다는 점을 명시하고, "더 적절해 보인다"의 기준이 서비스 레벨 SRP라는 별개의 축임을 분명히 해 두 주장의 관계를 정리한다.

### 🟡 Medium · 오타 — 28행 "요구사항을 구체화 하면서 유스케이스가 점점 세분화 됐습니다."

'-화하다', '-화되다'는 접미사가 결합한 한 단어이므로 붙여 써야 함. '구체화 하면서', '세분화 됐습니다'는 띄어쓰기 오류.

**제안:** "구체화하면서", "세분화됐다(됐습니다)"로 붙여 쓴다.

### 🟡 Medium · 오타 — 493행 "다양한 아키텍쳐의 원칙"

외래어 표기법상 'architecture'의 올바른 표기는 '아키텍처'임. '아키텍쳐'는 비표준 표기.

**제안:** "아키텍처"로 수정.

### ⚪ Low · 코드 — 261행 "creation <-- creation: showtimes", 431행 "creator <-- creator: showtimes"

바로 윗줄에서 showtimes 서비스에 createShowtimes를 호출했으므로 응답 화살표의 출발점은 showtimes여야 하는데 자기 자신에게서 받는 것으로 그려져 있음. 같은 글의 218행("creation <-- showtimes: showtimes") 표기와도 불일치.

**제안:** 각각 "creation <-- showtimes: showtimes", "creator <-- showtimes: showtimes"로 수정.

### ⚪ Low · 일관성 — 270행 "createTicketCreateDto", 272행 "ticketCreateDtos" vs 439행 "buildCreateTicketDto", 441행 "createTicketDtos"

리팩토링 전후로 같은 로직을 그린 다이어그램인데 동일한 메서드/변수가 다른 이름(createTicketCreateDto ↔ buildCreateTicketDto, ticketCreateDtos ↔ createTicketDtos)으로 표기되어 있음.

**제안:** 두 다이어그램의 명칭을 buildCreateTicketDto / createTicketDtos 한 쌍으로 통일.

### ⚪ Low · 일관성 — 534행 "worker <- TCreator: { createdShowtimeCount, createdTicketCount }"

티켓 생성만 담당하도록 분리한 TicketBulkCreatorService가 여전히 createdShowtimeCount를 반환하는 것으로 그려져 있음. 분리 전 다이어그램(444행)을 복사하면서 남은 잔재로 보이며, 분리 의도와 그림이 어긋나 독자가 혼동할 수 있음.

**제안:** TCreator의 반환값을 "{ createdTicketCount }"로 수정.

### ⚪ Low · 일관성 — 1-10행 (글 서두, "## 0. 지금까지")

3편 결론(808행)은 "다음 시간에는 `ShowtimeCreationService`의 테스트에 대해서 이야기 하겠다"고 예고했는데, 다음 글인 3.5편은 테스트가 아닌 단일 책임 원칙·동기/비동기 호출에 대한 보충 글이다. 3.5편 어디에도 이 글이 예고를 끊고 들어온 번외/보충 회차라는 안내가 없어, 3편의 예고를 따라온 독자가 혼란을 겪는다.

**제안:** 3.5편 서두에 "테스트(4편)로 넘어가기 전에 짚고 갈 내용이 있어 3.5편을 끼워 넣는다"는 안내 문장을 추가하거나, 3편의 예고 문장을 수정한다.

### ⚪ Low · 문법 — 449행 "여기까지 1,2,3편에서 다뤘던 내용이다."

주격 조사가 빠져 문장이 어색하고('여기까지가'), 숫자 나열도 쉼표 뒤 띄어쓰기가 없음.

**제안:** "여기까지가 1, 2, 3편에서 다뤘던 내용이다."로 수정.

### ⚪ Low · 구조 — 546~567행과 631~652행: "MSA에서 가장 간단한 비동기 서비스 호출" 다이어그램

동일한 비동기 호출 다이어그램이 두 번 거의 그대로 반복됨(차이는 565행 '(4) DONE/FAIL + result' vs 650행 '(4) DONE/FAIL'뿐). 특히 첫 번째(546행)는 비동기 메시징 설명(569행)보다 앞에 등장해 맥락 없이 제시됨.

**제안:** 첫 번째 다이어그램을 569행 설명 뒤로 옮기거나 둘 중 하나를 제거하고, 남기는 쪽의 라벨('DONE/FAIL + result' 여부)을 통일.

### ⚪ Low · 문체 — 492행 "...반영하는 것이 최우선적으로 지켜야 할 원칙이다.(본질기반해석이란 얘기다)"

마침표 뒤에 띄어쓰기 없이 괄호가 바로 붙어 있고, 괄호 안 '본질기반해석'도 모두 붙여 써서 가독성이 떨어짐.

**제안:** "...지켜야 할 원칙이다(본질 기반 해석이란 얘기다)."처럼 괄호를 문장 안으로 넣고 마침표를 뒤로 옮기거나, 괄호 내용을 별도 문장으로 분리.

### ⚪ Low · 문체 — 538행 "그러나 지금까지 경험으로는 대체로 시간은 항상 부족했다."

'대체로'와 '항상'이 한 문장에서 충돌해 의미가 모호함(대체로 부족했다는 것인지 항상 부족했다는 것인지). 또한 '지금까지 경험으로는'은 '지금까지의 경험으로는'이 자연스러움.

**제안:** "그러나 지금까지의 경험으로는 시간은 항상 부족했다." 또는 "~대체로 시간이 부족했다." 중 하나로 정리.

### ⚪ Low · 문체 — 629행 "동기식은 이렇게 간단하게 호출할 수 있지만 장애 전파를 막겠다고 비동기식 호출을 도입한다면"

문장이 조건절('~도입한다면')로 끝난 채 다이어그램으로 넘어가 글로는 미완결 문장임. 귀결은 673행("이렇게 복잡해진다")에 가서야 나와 독자가 흐름을 놓칠 수 있음.

**제안:** "~비동기식 호출을 도입한다면 다음과 같이 복잡해진다."처럼 문장을 완결하고 다이어그램을 제시.

### ⚪ Low · 오타 — 660행 "// readFileSync을 호출하면 syncText에 값이 할당된다."

'readFileSync'는 '싱크'로 읽혀 모음으로 끝나므로 목적격 조사는 '를'이 맞음. '을'은 조사 선택 오류.

**제안:** "readFileSync를 호출하면"으로 수정.

### ⚪ Low · 오타 — 538행 "이것은 프로젝트 상황 마다 다르기 때문에"

'마다'는 보조사이므로 앞말에 붙여 써야 함.

**제안:** "프로젝트 상황마다 다르기 때문에"로 수정.

### ⚪ Low · 오타 — 542행 "Worker–ShowtimeCreator–TicketCreator 는 동기식 통신을 하고 있어서"

조사 '는'이 앞말과 띄어져 있음. 조사는 앞말에 붙여 써야 함.

**제안:** "Worker–ShowtimeCreator–TicketCreator는"으로 붙여 쓴다.

### ⚪ Low · 오타 — 673행 "TheatersService같은 코어 서비스", "알리는 것 뿐이다"

'같은'은 형용사이므로 앞 명사와 띄어 써야 하고('TheatersService 같은'), '뿐'은 체언('것') 뒤에서는 조사이므로 붙여 써야 함('것뿐이다').

**제안:** "TheatersService 같은 코어 서비스", "알리는 것뿐이다"로 수정.

### ⚪ Low · 오타 — 675행 "1인 개발을 가정한 프로젝트 치고는", 678행 "알아서 잘 할테니까"

'치고'는 조사이므로 붙여 써야 하고('프로젝트치고는'), '테니까'의 '터'는 의존명사이므로 '할 테니까'로 띄어 써야 함.

**제안:** "프로젝트치고는", "알아서 잘할 테니까"로 수정.

### ⚪ Low · 오타 — 371행 "worker -> worker: createsagaId"

다이어그램 라벨이 'createsagaId'로 붙어 있어 오타로 보임(다른 곳에서는 sagaId 표기 사용).

**제안:** "createSagaId" 또는 "create sagaId"로 수정.

### ⚪ Low · 오타 — 225행 "ShowtimeCreationService에서 시작되는 메소드 호출이"

외래어 표기법상 표준 표기는 '메서드'임.

**제안:** "메서드"로 수정.

## 2026-06-18-backend-design-4.md (31건)

### 🔴 High · 코드 — 103행 "constructor(db:MySQL){}" / 106행 "this.db.find" (동일 패턴: 130, 151, 173, 184, 193, 280, 289, 298, 443, 454, 463행)

TypeScript에서 생성자 매개변수에 접근 제한자(private/public 등)가 없으면 클래스 프로퍼티로 등록되지 않는다. 본문 코드처럼 `constructor(db:MySQL){}`로 선언하면 `this.db`가 undefined가 되어 `this.db.find(...)` 호출 시 런타임 TypeError가 발생한다. 글의 모든 서비스 클래스(ShowtimesService, ValidatorService, CreatorService)가 같은 패턴이어서 예제 코드 전체가 실제로는 동작하지 않는다.

**제안:** 모든 생성자를 `constructor(private db: MySQL) {}`, `constructor(private showtimesService: ShowtimesService) {}` 형태로 수정하거나, 본문에 `this.db = db` 할당을 명시한다.

### 🔴 High · 코드 — 132-135행 "const conflicts = this.showtimesService.find(theaterId, startTime, duration)" / "if (conflicts.length > 0) throw new Error('conflict')" (동일 패턴: 153-156, 186-189, 195-198, 291-294, 456-459, 465-469행)

main()에서 `await showtimesService.find(...)`로 호출하는 것을 보면(121, 207행) find/save는 Promise를 반환한다. 그런데 ValidatorService.validate는 await 없이 find를 호출하므로 `conflicts`는 Promise 객체이고 `conflicts.length`는 항상 undefined여서 충돌 검증이 절대 동작하지 않는다(에러를 던지는 일이 없음). CreatorService.create도 validate/save를 await 없이 호출한다. 글의 핵심 예제인 '충돌 검증' 로직이 코드상으로는 전혀 작동하지 않는 상태다.

**제안:** validate와 create를 async로 선언하고 `const conflicts = await this.showtimesService.find(...)`, `await this.validatorService.validate(...)`, `await this.showtimesService.save(...)`처럼 await를 추가한다.

### 🔴 High · 구조 — 파일명 날짜 2026-06-18 (front matter 1-4행에는 date 필드 없음, title: 백엔드 서비스 분석과 설계 (4))

오늘(2026-06-11) 기준 미래 날짜 글이다. _config.yml과 빌드 명령(.github/workflows/jekyll.yml의 `bundle exec jekyll build -t -V`) 어디에도 future: true 설정이 없어 Jekyll 기본값(future: false)에 따라 이 글은 빌드에서 제외된다. 게다가 배포 워크플로 트리거가 push/workflow_dispatch뿐이라 2026-06-18이 지나도 새 push가 없으면 자동으로 게시되지 않는다.

**제안:** 의도적 예약 글이라면 _config.yml에 future: true를 추가하거나 게시 시점에 맞춰 빌드를 다시 트리거하는 schedule 워크플로를 추가한다. 즉시 게시가 의도라면 파일명 날짜를 오늘 이전으로 수정한다.

### 🟡 Medium · 코드 — 424-430행 "@Route('showtime-creation') class ShowtimeCreationController { @Post('showtimes') createShowtimes(body) { return this.creator.create(...) } }", 433행 "실행은 된다"

ShowtimeCreationController에 생성자가 없어서 `this.creator`가 어디에서도 주입/할당되지 않는다. 이 상태로는 createShowtimes 호출 시 `this.creator`가 undefined여서 즉시 TypeError가 발생하므로, 바로 다음 문장의 "CreatorService 내부는 아직 stub이지만 실행은 된다"(433행)는 설명과 코드가 일치하지 않는다.

**제안:** `constructor(private creator: CreatorService) {}`를 추가하여 의존성 주입을 명시한다.

### 🟡 Medium · 코드 — 683행 "jest.mock('../model', () => ({ useModel: () => mockModel }))", 687행 "mockModel = { isError: false, ...values }", 696-699행 "test('error states', ... renderScreen({ isError: true })"

스냅샷 테스트 예제에 세 가지 문제가 있다. (1) `mockModel`과 `mockProps`가 어디에도 선언되어 있지 않아 코드가 그대로는 실행되지 않는다. (2) mock 값으로 쓰는 `isError`는 실제 useModel의 반환값(email, emailAlerted, canContinue, doContinue, goBack, onEmailChanged — 665-676행)에 존재하지 않는 필드다. (3) 뷰 컴포넌트 SignupStep1(650-662행)은 `M.isError`를 전혀 사용하지 않으므로 'error states' 스냅샷은 'default states' 스냅샷과 동일하게 나온다. 결국 "에러 상태일 때 화면이 어떻게 보이는가"(646행)를 검증한다는 주장을 예제가 입증하지 못한다.

**제안:** `let mockModel: any`와 `mockProps` 선언을 추가하고, 뷰가 실제로 사용하는 상태(예: emailAlerted)를 mock해서 화면이 달라지는 스냅샷을 보여주도록 예제를 일치시킨다. 예: renderScreen({ emailAlerted: true }) 후 EmailInput의 alerted 표시가 스냅샷에 반영되는 형태.

### 🟡 Medium · 일관성 — 363-393행 "usecase \"티켓 예매하기\" as UC2 / usecase \"티켓 구매하기\" as UC3 ... BookingService ... PurchaseService"

top-down 설계 흐름을 보여주는 다이어그램에 `티켓 예매하기`(/booking/*, BookingService)와 `티켓 구매하기`(/purchases/*, PurchaseService)가 별개 유스케이스로 등장한다. 그러나 1편(203행)에서 도메인 전문가와의 문답으로 "영화 예매하기 = 티켓 구매하기"로 통합 정리했고, 이후 1~3.5편의 유스케이스 목록에는 `티켓 구매하기`와 `티켓 환불하기`만 존재한다. 시리즈에서 정의한 적 없는 유스케이스와 서비스가 갑자기 나타나 이전 글들과 모순된다.

**제안:** 다이어그램의 UC2/UC3를 시리즈에서 정의한 유스케이스(예: `티켓 구매하기`, `티켓 환불하기`)로 교체하고 대응 서비스 이름도 그에 맞춰 수정한다. 새 유스케이스를 의도적으로 추가한 것이라면 본문에 그 배경을 설명한다.

### 🟡 Medium · 일관성 — 30-35행 "component \"ValidatorService\" as AValidator / component \"CreatorService\" as ACreator / AService --> AWorker : 3. enqueueShowtimeCreationJob"

이 컴포넌트 다이어그램은 "지금까지 작성한 시퀀스 다이어그램"(10행)의 구조를 옮긴 것이라고 하면서도, 3·3.5편에서 확정한 클래스명 `ShowtimeBulkValidatorService`/`ShowtimeBulkCreatorService` 대신 `ValidatorService`/`CreatorService`를 사용한다. 호출명도 3편의 `requestShowtimeCreation`+`enqueueJob`이 아닌 `enqueueShowtimeCreationJob`으로 바뀌었다. 호출 구조 단순화 안내(72행)는 이 다이어그램 이후에 나오므로 이 다이어그램에는 적용되지 않는다.

**제안:** 첫 컴포넌트 다이어그램은 3편에서 확정한 이름(ShowtimeBulkValidatorService, ShowtimeBulkCreatorService, requestShowtimeCreation 등)을 그대로 사용하고, 단순화한 이름은 "단순화하겠다"고 안내한 두 번째 다이어그램부터 사용한다.

### 🟡 Medium · 문법 — 339행: "그렇다고 유닛 테스트가 함수를 테스트하는 건데 이게 틀린 방법은 아닌 것 같다."

비문이다. '그렇다고 ~하는 건데 이게 틀린 방법은 아닌 것 같다'는 접속과 주술 구조가 꼬여 있어 문장이 성립하지 않고, 독자가 의미를 추측해야 한다.

**제안:** "그렇다고 해서 '유닛 테스트는 함수를 테스트하는 것'이라는 생각이 틀린 것 같지도 않다." 정도로 고친다.

### 🟡 Medium · 논리 — 210행 "await showtimesService.save(1, 1, '2026-01-01 10:00', 120)" → 215행 "await validatorService.validate(1, '2026-01-01 10:00', 120)" → 216행 "console.log('validation passed')" (테스트 함수 버전: 236-248행)

예제 데이터가 자기모순이다. 같은 극장(theaterId=1), 같은 시작 시간, 같은 duration으로 상영시간을 저장한 직후 동일한 값으로 validate를 호출하므로, 코드가 의도대로 동작한다면 find가 방금 저장한 행을 찾아 'conflict' 에러를 던져야 하고 'validation passed'는 출력될 수 없다. 이어지는 create(220행)도 같은 이유로 실패해야 한다. 본문에서는 모든 단계가 성공하는 것처럼 서술되어 있는데, 이는 위에서 지적한 await 누락 버그 때문에 검증이 무력화된 결과와만 일치한다.

**제안:** validate/create 테스트에는 충돌하지 않는 다른 시간대(예: 다른 날짜나 다른 theaterId)를 사용하거나, 충돌 케이스임을 명시하고 에러가 발생하는 흐름으로 서술을 바꾼다.

### 🟡 Medium · 논리 — 420행 "return { sagaId: 123 }" vs 465-468행 "create(...) { ... const showtime = this.showtimesService.save(...); return showtime }", 480행 "실행 방법은 바뀌지 않는다"

top-down Step 1의 stub은 시퀀스 다이어그램(407행 "{ sagaId }")과 일치하게 `{ sagaId: 123 }`을 반환하지만, '모든 구현이 완료됐다'는 Step 2의 최종 CreatorService.create는 showtime 객체를 반환한다. 즉 REST API 응답 계약이 Step 1과 Step 2 사이에서 바뀌었는데, 본문은 "인터페이스가 바뀌는 건 아니니까"(413행), "2단계에 걸쳐 구현이 진행됐지만 실행 방법은 바뀌지 않는다"(480행)라고 주장한다. top-down의 핵심 장점(인터페이스 고정)을 보여주는 예제가 스스로 인터페이스를 바꾸고 있어 주장과 모순된다.

**제안:** Step 2의 create도 `{ sagaId }`(또는 일관된 응답 형태)를 반환하도록 수정하여 stub과 최종 구현의 응답 계약을 동일하게 유지한다.

### 🟡 Medium · 논리 — 333행 "4개의 테스트가 모두 실패한다." (테스트 코드: 312-331행)

제시된 테스트 함수들은 단언(assertion) 없이 호출과 console.log만 수행하므로 예외가 발생하지 않는 한 실패할 수 없다. 또한 JS/TS에서 매개변수 이름이 duration에서 endTime으로 바뀌어도 위치 기반 호출(`find(1, '2026-01-01 10:00', 120)`)은 그대로 실행되며, 120이 endTime 자리에 전달되어 조용히 잘못된 의미로 동작할 뿐 테스트가 '실패'하지는 않는다. 매개변수에 타입이 선언되어 있어야 컴파일 에러로 실패하는데 예제 코드는 매개변수가 무타입이라 그 전제도 성립하지 않는다. 섹션 1.2의 핵심 주장을 예제가 뒷받침하지 못한다.

**제안:** 메서드 매개변수에 타입을 명시해 시그니처 변경 시 테스트 코드가 컴파일 에러로 깨지는 모습을 보여주거나, expect 같은 단언이 포함된 테스트로 바꿔 실제 실패가 발생하는 구조로 예제를 수정한다.

### 🟡 Medium · 논리 — 613행 "### 2.4. mock은 내가 만들지 않은 객체에만 쓴다" / 619행 "내가 생성하지 않은 객체를 조작해야 할 때다" vs 679-683행 "모델을 mock해서 ... jest.mock('../model', () => ({ useModel: () => mockModel }))"

섹션 2.4는 'mock은 내가 만들지 않은 객체(외부 라이브러리, 다른 팀의 모듈)에만 쓴다'고 원칙을 세우고 결론(709행)에서도 반복하지만, 섹션 2.5에서는 자신이 직접 작성한 useModel 모듈을 jest.mock으로 mock한다. 글 내부에서 원칙과 실제 예제가 충돌하며, 이 예외가 왜 허용되는지에 대한 설명이 없다.

**제안:** 2.5에서 자기 코드인 모델을 mock하는 것이 2.4 원칙의 예외인 이유(스냅샷 테스트의 대상이 뷰 렌더링이므로 모델을 테스트 경계 밖으로 분리한다는 점 등)를 명시하거나, 2.4의 원칙 서술을 '테스트 대상 경계 밖의 의존성에만 쓴다'로 다듬는다.

### 🟡 Medium · 구조 — 343행: "실행과 검증도 어렵다. 테이블을 만들었으면 ... 앞서 본 main() 함수가 바로 그 임시 코드다."

문단 위치가 어색하다. 341행에서 "이에 대해서는 뒤에서 더 자세히 다룬다."로 unit 논의를 마무리한 직후 갑자기 bottom-up의 다른 단점(실행/검증의 어려움)이 등장해 흐름이 끊긴다. '도'가 무엇에 이어지는지도 불분명하다.

**제안:** 이 문단을 bottom-up의 단점을 논하는 앞부분(337행 부근)으로 옮기거나, "bottom-up에는 또 다른 문제도 있다. 실행과 검증이 어렵다는 점이다."처럼 연결 문장을 추가한다.

### 🟡 Medium · 구조 — 511행: "<!-- TODO 이 부분 더 자세하게 쓰자. 스텁을 작성한다는 건 뭔가? 그림으로 표현하자 -->", 594행: "<!-- TODO 여기에 github action에서 e2e 실행하는거 보여주자 -->"

초안용 TODO 주석 2개가 남아 있다. 렌더링 화면에는 보이지 않지만 게시되는 HTML 소스에 그대로 노출되고, 글이 미완성 상태임을 나타낸다.

**제안:** 게시 전에 TODO 내용을 본문에 반영하거나 주석을 삭제한다.

### 🟡 Medium · 기술 오류 — 105-107행 "find(theaterId, startTime, duration) { return this.db.find({ theaterId, startTime, duration }) }" vs 502-504행 "# 충돌 검증 ... \"startTime\":\"2026-01-01 10:30\""

find가 theaterId/startTime/duration의 완전 일치(exact match) 조건으로 조회하므로 '시간이 겹치는' 상영시간을 찾을 수 없다. 섹션 2.1의 curl 테스트는 10:00에 120분짜리를 생성한 뒤 10:30 요청을 '충돌 검증'이라고 표시하지만, 본문에 제시된 구현으로는 startTime이 '10:30'과 정확히 일치하는 행만 조회하므로 이 충돌은 절대 탐지되지 않는다. 컴포넌트 다이어그램의 findConflicts(61행)라는 이름이 의미하는 구간 겹침 검사와도 어긋난다.

**제안:** find를 구간 겹침 조건(예: 기존 startTime < 신규 endTime AND 기존 endTime > 신규 startTime)으로 조회하는 형태로 바꾸거나, 단순화한 코드라는 점과 실제로는 범위 검색이 필요하다는 점을 본문에 명시한다.

### ⚪ Low · 코드 — 648행, 681행 코드 블록(```ts)에 JSX 포함: "return (<View>...", "render(<SignupStep1 {...mockProps} />)"

JSX가 들어 있는 코드 블록의 언어 지정이 'ts'라서 문법 강조가 깨질 수 있다.

**제안:** 두 코드 블록의 언어 지정을 'tsx'로 바꾼다.

### ⚪ Low · 일관성 — 266행 "유닛테스트", 602행 "유닛테스트/통합테스트" vs 272, 600, 604, 607, 609, 611행 "유닛 테스트"

같은 글 안에서 '유닛테스트'와 '유닛 테스트' 표기가 섞여 있다.

**제안:** 한 가지 표기('유닛 테스트')로 통일한다. '통합테스트'도 동일하게 '통합 테스트'로 맞춘다.

### ⚪ Low · 일관성 — 509행 "curl 기반의 쉘 스크립트" vs 546행, 592행 "쉘스크립트"

'쉘 스크립트'와 '쉘스크립트' 표기가 섞여 있다.

**제안:** 한 가지 표기(예: '셸 스크립트' 또는 '쉘 스크립트')로 통일한다.

### ⚪ Low · 일관성 — 266행 "Jest나 JUnit" vs 615행 "jest는 성검이 아니라", 642행 "jest나 testing-library"

본문 산문에서 제품명 표기가 'Jest'와 'jest'로 오락가락한다. 433행 'stub'과 513행 '스텁 코드'도 영문/한글 표기가 섞여 있다.

**제안:** 산문에서는 제품명을 'Jest'로 통일하고, stub도 '스텁' 한 가지 표기로 통일한다.

### ⚪ Low · 일관성 — 351, 364, 411, 490행 "상영시간" vs 403행 "기존 상영 시간 조회", 406행 "상영 시간 생성 요청"(시퀀스 다이어그램)

본문은 '상영시간', 다이어그램 안 텍스트는 '상영 시간'으로 띄어쓰기가 다르다.

**제안:** 다이어그램 텍스트도 본문과 같이 '상영시간'으로 통일한다.

### ⚪ Low · 일관성 — 636행 제목 "프론트는 스냅샷으로", 638, 640, 644행 "프론트" vs 513, 592, 709행 "프론트엔드"

같은 개념을 '프론트'와 '프론트엔드'로 섞어 부른다.

**제안:** '프론트엔드'로 통일하거나, 최소한 절 제목에서는 '프론트엔드'를 쓴다.

### ⚪ Low · 일관성 — 521-529행 "\"rating\": \"PG\", \"assetIds\": []" (curl 테스트 스크립트 예시)

영화 생성 curl 예시의 필드가 3편에서 정의한 `Movie` 엔티티와 어긋난다. 3편(76-77행)은 `rating: MovieRating { PG13, R, NC17 }`과 `imageIds: ObjectId[]`로 정의했는데, 4편 예시는 enum에 없는 `"PG"`와 `assetIds`라는 다른 필드명을 사용한다. "실제 프로젝트에서 사용하는 스크립트"라는 단서가 있긴 하지만 같은 영화 예매 도메인 예시라 독자가 같은 시스템으로 읽기 쉽다.

**제안:** 예시를 3편 엔티티 정의에 맞춰 `"rating": "PG13"`, `"imageIds": []`로 수정하거나, 시리즈의 설계와 별개인 다른 프로젝트의 스크립트임을 본문에 더 명확히 밝힌다.

### ⚪ Low · 문법 — 72행: "호출구조를 단순화 해서 설명하겠다", 411행: "시퀀스 다이어그램으로 구체화 할 수 있었다"

'-화하다'는 한 단어이므로 붙여 써야 한다. '단순화 해서', '구체화 할'은 띄어쓰기 오류다.

**제안:** 각각 "단순화해서", "구체화할"로 붙여 쓴다.

### ⚪ Low · 문법 — 270행: "보통 본문 코드 만큼이면 적절한 것 같다고"

체언 뒤의 '만큼'은 조사이므로 붙여 써야 한다.

**제안:** "본문 코드만큼이면"으로 붙여 쓴다.

### ⚪ Low · 문체 — 268행: "모든 함수마다 테스트가 있으니 함수들이 제대로 잘 동작할 거라는 안심이 든다."

'모든 ~마다'는 의미가 중복된 겹말이고, '안심이 든다'는 비표준 연어다(보통 '안심이 된다' 또는 '생각이 든다').

**제안:** '~거라는 안심이 된다'도 관형절과 '안심'의 결합이 어색하다. "함수마다 테스트가 있으니 제대로 동작할 거라고 안심하게 된다." 또는 "함수마다 테스트가 있으니 제대로 동작하리라는 생각에 안심이 된다."로 고친다.

### ⚪ Low · 문체 — 276행: "요구사항이 변경되어 `duration` 대신 `endTime`을 받도록 바뀌었다고 하자."

'변경되어'와 '바뀌었다'가 중복된다.

**제안:** "요구사항이 변경되어 `duration` 대신 `endTime`을 받게 되었다고 하자." 또는 "요구사항이 `duration` 대신 `endTime`을 받는 것으로 바뀌었다고 하자."로 고친다.

### ⚪ Low · 문체 — 351행: "\"상영시간을 관리하고 싶다\"는 있지만"

인용 뒤에 받쳐 줄 명사가 빠져 '~다"는 있지만'이 불완전하게 읽힌다.

**제안:** "\"상영시간을 관리하고 싶다\"는 생각은 있지만"처럼 명사를 보충한다.

### ⚪ Low · 문체 — 611행: "각각의 객체 역할은 작고 분명하다. 이런 메소드까지 모두 유닛 테스트를 작성하는 것은 극히 비효율적이다."

앞 문장은 '객체'를 말했는데 다음 문장에서 갑자기 '이런 메소드'를 가리켜 지시 대상이 불분명하다. 또한 이 글에서 유일하게 '메소드' 표기를 쓰는데 표준 표기는 '메서드'다.

**제안:** "이렇게 단순한 메서드까지 모두 유닛 테스트를 작성하는 것은 극히 비효율적이다."처럼 지시 대상을 분명히 하고 '메서드'로 표기한다.

### ⚪ Low · 문체 — 341행: "**하나의 동작(behavior)** 이다" vs 604행 "**비용 대비 효과**다"

341행은 굵게 표시 뒤에 불필요한 공백을 두어 렌더링 시 '동작(behavior) 이다'처럼 서술격 조사 앞이 띄어진다. 604행은 붙여 써서 같은 패턴의 표기가 일관되지 않다.

**제안:** kramdown에서는 "**하나의 동작(behavior)**이다"처럼 붙여 써도 굵게 처리되므로 공백을 제거해 604행과 통일한다.

### ⚪ Low · 기술 오류 — 268행 "테스트가 실패하면 어떤 함수에 문제가 있는지 즉시 알 수 있다. `test_ValidatorService_validate`가 실패하면 ValidatorService에 문제가 있다는 뜻이니까."

제시된 테스트들은 격리되어 있지 않다. test_ValidatorService_validate는 실제 ShowtimesService와 실제 DB를 거치므로, 이 테스트의 실패가 ValidatorService의 문제라고 단정할 수 없다(ShowtimesService 버그, DB 연결 문제, 앞선 save 테스트가 남긴 데이터 등도 원인이 될 수 있다). bottom-up 방식의 장점으로 단정적으로 서술되어 있어 기술적으로 부정확하다.

**제안:** '실패 지점을 즉시 알 수 있을 것 같다'는 인식이 의존성이 격리되지 않은 이 구조에서는 실제로 성립하지 않는다는 점을 함께 짚어주거나, 해당 문장을 '~알 수 있을 것처럼 보인다'로 완화해 뒤의 반박과 연결한다.

### ⚪ Low · 오타 — 351행: "도메인 전문가의 머리속에 있는 추상적인 생각을"

'머리속'은 틀린 표기다. 사이시옷을 받친 '머릿속'이 맞다.

**제안:** "머리속"을 "머릿속"으로 수정한다.
